# YouTube Music 대표 ID 안정화 — 구현·검증 로드맵

작성일: 2026-09-14 · 구현·검증 갱신: 2026-09-15 · 상태: 예방 코드 배포·DB 24건 적용 완료, 운영 최종 검증 NO-GO · 대상: 재발 방지 구현, 이미 발생한 오류의 교정, 운영 검증

목표는 정상 재생되던 동일 곡의 대표 ID가 차트 갱신이나 후처리 때문에 불필요하게 바뀌지 않게 하는 것이다. 기존 ID가 실제로 사용할 수 없게 된 경우에는 동일 곡의 검증된 대체 ID로 일관되게 전환한다. 인증·네트워크·실행 환경의 불확실성 때문에 정상 데이터를 교체하거나 곡을 누락시키지 않는다.

**재검토 결론: 예방 구현과 일반 재실행만으로 이전 오류가 모두 해소되지는 않는다.** 이미 채택된 정상 ID의 잘못된 교체, 복구 선택을 거부하는 저장 경로, 최신 날짜만 갱신하는 history, 잘못된 NEW·추이 표시, 병합된 연결, 부분 게시 실패에는 별도 복구가 필요하다. 이번 개정은 이를 §9의 사건별 복구 절차와 독립 합격 기준으로 포함한다. 근거가 부족한 과거 상태나 현재 재생을 확인할 수 없는 ID는 복구 완료로 간주하지 않는다.

이 문서의 최초 계획에 따라 제품 코드를 구현하고 main에 배포했다. 운영 DB 24건은 적용했지만, 실제 목록의 자동 ID 치환 때문에 전체 복구는 보류됐다. 아래 초기 관측은 당시의 기준선이며, 최신 실제 적용 상태와 추가 로드맵은 §19와 [최종 검증 보고서](YTMusic_ID_Stability_Final_Verification.md)에 기록한다.

## 1. 기준선과 남은 문제

| 구분 | 확인 내용 | 계획에서의 취급 |
|---|---|---|
| 실행 이력 | 9/13에는 월요일 전용 주간 작업을 건너뛰었고, 9/14에는 실행했다. 두 실행의 제품 코드와 설치된 ytmusicapi 버전은 같았다. | 같은 주간 입력을 고정하여 실행 경로 차이를 재현한다. |
| ID 교체 | 원본 서비스 URL로 동일성을 확인한 교체는 13곡이다. | 13곡을 모두 재생 불가로 간주하지 않는다. |
| 로컬 인증 실측 | 제보된 기존 5개 ID는 모두 OK. 새 ID 중 Pop Off Pop Off, 404, Less than a Lover는 UNPLAYABLE. REDRED와 RUDE!의 새 ID는 OK지만 다른 앨범이다. | 현재 상태를 단정하는 영구 목록이 아니라 당시 응답 fixture로 사용한다. |
| 공개 조회 | 기존 정상 ID도 Premium 안내와 함께 UNPLAYABLE을 반환했다. | 공개 응답으로 인증 환경의 재생 가능 여부를 대신 판정하지 않는다. |
| 현재 로컬 패치 | 기존 대표 ID 보존, 차트의 non-OK ID 제외, 검색 재선택 방지, 관계 복구 보호, 동기화 전 테스트를 추가했다. | 최종 운영 구현의 출발점이다. 아직 운영 반영 전이다. |
| 검증 범위 | 기존 오프라인 202개와 추가 11개가 로컬에서 통과했다. 깨끗한 파일 구성에서도 추가 11개가 통과했다. | Actions에서 실제 곡을 검증한 결과나 213개 전체가 CI에서 실행된다는 뜻은 아니다. |
| 복구 저장 재현 | 메모리 SQLite에서 정상 복구 ID를 요청해도 단일·bulk 저장이 기존 오염 ID를 유지했다. bulk는 attempt의 선택 ID와 canonical이 다른데도 run이 completed였다. | 재검색 성공을 복구 성공으로 인정하지 않는다. 명시적 복구 저장과 commit 전 재조회가 필요하다. |
| 출력 간 차이 | 저장된 9/14 관측에서 404의 Weekly 목록은 이미 이전 정상 ID였지만 history는 새 ID였다. 현재 운영 DB는 복구 계획 확정 전에 별도 재조회해야 한다. | 곡별뿐 아니라 DB·목록·날짜별로 수정 필요 여부를 판정한다. |
| 과거 화면 | history 기본 export는 기존 파일이 있으면 최신 날짜 하나만 재계산한다. 프런트의 등락·추이는 video ID로 연결하며 metadata는 날짜 사이에 공유된다. | 이전 날짜 교정과 표시 검증을 별도 구현한다. ranking JSON 불변만으로 과거 화면 보존을 주장하지 않는다. |

추가로 해결할 문제:

1. 현재 `status != OK`는 실제 이용 불가, 로그인 요구, Premium 안내, 상태 누락을 한 종류로 처리한다.
2. 기존 대표 ID를 무조건 보존하면 이미 오염된 대표 ID도 고정될 수 있다.
3. 재생 가능한 대체 ID를 검색해도 단일·일괄 저장 경로가 기존 대표 ID를 유지하여 선택 결과와 DB가 달라질 수 있다.
4. `heal_split_tracks.py`의 Pass 3는 대표 ID를 `updated_at` 최신순으로 고른다. 차트 단계의 보호를 나중에 무효화할 수 있다.
5. 현재 후처리는 YouTube Music 게시 이후 실행된다. DB의 대표 ID가 게시 후 바뀌면 플레이리스트·Hype·history가 서로 다른 상태를 사용할 수 있다.
6. `get_existing_playlist_items()`는 `videoId`와 `setVideoId`만 남긴다. 원 응답의 `isAvailable`은 기존 소유권·순서 검사에 보존되지 않는다.
7. API 조회 전에 raw snapshot을 교체하고 이전 completed run을 stale로 만드는 경로가 있다. 판정 실패 시 마지막 정상 snapshot이 유지되는지 별도로 보장해야 한다.
8. REDRED·RUDE!처럼 이미 새 ID도 정상인 경우에는 일반 보존 정책이 잘못된 선택을 고정한다. 사건 증거로 제한된 정상→정상 복원 경로가 필요하다.
9. 현재 history exporter의 기본 갱신 또는 전체 재계산만으로는 영향 날짜만 정확히 교정할 수 없다. 원본 순위를 보존하면서 잘못된 파생 ID·표시·필요한 점수/순위를 정정해야 한다.
10. UID 병합은 과거 UID와 일부 metadata를 삭제·재배치한다. 정확한 역복원에 필요한 증거가 남았는지 확인해야 하며, 현재 DB만으로 모든 과거 상태를 복원할 수 있다고 가정하지 않는다.

## 2. 반드시 유지할 동작 규칙

- **곡 동일성과 재생 가능성을 분리한다.** Charts의 ATV 관계, 제목, 가수, 앨범은 동일 곡의 근거이지 재생 성공 증거가 아니다.
- **정상 기존 ID를 우선한다.** 기존 ID와 새 ID가 모두 정상이어도 새 차트 관계나 최신 갱신 시각만으로 교체하지 않는다.
- **복구 가능한 보존이어야 한다.** 기존 ID가 이용 불가로 재확인되고 대체 ID가 동일 곡·재생 검증을 통과하면 변경을 허용한다.
- **UNKNOWN은 곡 불가가 아니다.** 영구 차단, 캐시 삭제, 자동 재매칭, 무조건적인 곡 누락의 근거로 사용하지 않는다.
- **판정은 실행 환경과 시점에 한정한다.** 전 세계·모든 계정에서 영구적으로 불가능하다는 의미로 저장하지 않는다.
- **모든 자동 대표 ID 변경 경로에 같은 규칙을 적용한다.** 차트, 검색, 캐시, 저장, 관계 복구, 중복 병합이 포함된다.
- **수동 정책을 덮지 않는다.** block·split·override를 존중한다. 수동 지정 ID가 이용 불가라면 자동으로 다른 곡을 선택하지 말고 해당 게시를 보류한다.
- **저장된 결과와 게시할 결과를 일치시킨다.** 완료 상태는 함수가 반환한 선택 ID만으로 결정하지 않는다.
- **사건 복원은 좁은 예외다.** 잘못된 자동 교체의 전후 근거·현재 동일성/재생 검증·수동 정책 대조가 있는 명세에 한해 정상→정상 복원을 허용한다. 일반적인 오래된 ID 우선 규칙이나 영구 manual override로 만들지 않는다.
- **원본과 잘못된 파생 결과를 구분한다.** 검증된 raw 차트 순위·기간과 수정 전 증거는 보존한다. 오류의 영향을 받은 과거 history/Hype는 날짜를 명시해 교정하고 영향 밖 표시·곡·순위는 유지한다.

## 3. 재생 판정 모델

인증 상태, player 경로 상태, 개별 곡 판정을 따로 둔다. `run_health=healthy`에는 인증 유효성 확인과 기준곡 검사가 모두 필요하다. 아래 `playable`은 검증한 player 경로가 재생 가능하다고 선언했다는 뜻이며, 실제 사용자의 기기에서 오디오를 재생했다는 뜻은 아니다.

| 곡 판정 | 최소 근거 | 선택 정책 |
|---|---|---|
| `playable` | 정상으로 확인한 실행 환경, 정확한 요청 ID, 인증 player의 OK, 오디오 형식 정보 존재, 상충하는 availability 증거 없음 | 동일 곡 검증을 통과한 후보로 허용한다. 기존 정상 ID 교체를 자동 허용하지는 않는다. |
| `unavailable` | 정상 실행 환경에서 해당 곡에 대한 명확한 이용 불가 응답을 재확인. 정확한 ID의 `isAvailable=false`가 있으면 보강 근거로 기록 | 해당 실행에서 제외한다. 기존 정상 ID 또는 검증된 대체 ID를 사용한다. |
| `unknown` | 로그인 요구, 불명확한 Premium 안내, 인증·환경 불확실, 401/403/429·5xx·timeout·challenge, 파싱 실패, OK지만 오디오 정보 누락, 증거 충돌, 알려지지 않은 상태 | 새 대표 ID 승격을 보류한다. 기존 정상 선택으로 완전한 출력을 만들 수 없으면 해당 게시를 보류한다. |

세부 규칙:

1. HTTP 200, 제목·가수 정보 존재, 검색 결과 노출만으로 `playable`을 만들지 않는다.
2. `isAvailable=true`만으로 성공을 확정하지 않는다. 필드가 없으면 false가 아니라 미관측이다.
3. player OK와 `isAvailable=false`가 충돌하면 `unknown`으로 둔다.
4. 요청과 다른 ID가 반환되면 다른 곡의 metadata를 재사용하지 않는다. 동일성 오류로 거절하고 재생 판정도 확정하지 않는다.
5. 한국어·영어 reason 문자열 하나로 영구 차단하지 않는다. 명확하게 검증된 응답 패턴만 분류하고 나머지는 `unknown`이다.
6. 공개 조회와 인증 조회는 다른 관측이다. 두 결과를 섞어 성공으로 만들지 않는다.
7. 서명 URL이나 스트림 정보 존재만으로 실제 전달·디코딩 성공을 주장하지 않는다. 기본 구현에 다운로드, URL HEAD 검사, 토큰 우회, 프록시를 추가하지 않는다.

`get_song()`은 metadata와 streaming 정보를 반환하며, 문서에는 기본 `signatureTimestamp`가 유효하지 않은 스트림 URL을 만들 수 있다고 명시되어 있다. 따라서 오디오 정보는 보강 증거로 사용하고, Actions에서 정상 기준곡조차 그 정보를 안정적으로 제공하지 않으면 이 기준을 느슨하게 바꾸기 전에 CP1에서 중단·재설계한다. [ytmusicapi 문서](https://ytmusicapi.readthedocs.io/en/stable/reference/browsing.html#ytmusicapi.YTMusic.get_song)

### 실행 환경 확인

- 실제 동기화와 같은 인증 설정·client 설정으로 실행한다. 인증 파일 존재와 JSON 파싱 성공은 로그인 성공의 증거가 아니다. 브라우저 인증은 저장된 세션 헤더를 재사용한다. [인증 문서](https://ytmusicapi.readthedocs.io/en/stable/setup/browser.html)
- 계정에서 최근 확인한 서로 다른 아티스트의 기준곡 3개를 사용한다. 초기 후보는 이번 기존 ID에서 고를 수 있지만 영구 정상곡으로 하드코딩하지 않는다.
- 초기 기준은 3개 중 2개 이상에서 정확한 ID·OK·오디오 정보를 확인하고, 공통 인증 오류가 없는 것이다. 실패한 기준곡은 개별 문제인지 환경 문제인지 기록한다.
- 기준곡 통과는 player 경로가 작동한다는 증거다. 계정 신원이나 Premium 자격을 별도로 보장한다고 해석하지 않는다. 기준곡만 통과하고 인증 근거가 없으면 인증 상태는 UNKNOWN이며 후보 승격·곡 불가 확정을 보류한다.
- 인증 확인의 구현 후보는 `get_account_info()`다. 설치된 1.12.0 코드와 공식 문서에서 메서드 존재를 확인했으며, 계정 정보를 읽는 API다. CP1에서 정상 인증 응답과 비인증 대조군을 실제로 비교하여 상태 구분에 쓸 수 있는지 입증한다. 성공 응답 구조와 기대 프로필의 일치 여부만 검사하고 계정명·채널명·사진 URL은 기록하지 않는다. API 오류·식별 불확실은 UNKNOWN이며 Premium 자격까지 추론하지 않는다. [공식 API 문서](https://ytmusicapi.readthedocs.io/en/1.12.2/reference/api/ytmusicapi.mixins.html#ytmusicapi.mixins.library.LibraryMixin.get_account_info)
- 다른 3개 ID에서 연속 환경성 오류가 나면 기준곡을 재확인하고 남은 후보 처리를 중단한다.
- 한국어 응답 설정과 한국 사용자 접속 환경을 동일시하지 않는다. 기본 운영 판정 범위는 지정 계정의 Actions 실행 환경이다. 로컬과 Actions가 다르게 관측된 ID는 `environment_disagreement`로 보류하고 자동 변경하지 않는다. CP1에서 광범위한 차이가 확인되면 Actions 판정만으로 운영 전환을 허용하지 않는다.
- 한국 환경의 모든 신규 후보까지 보장하는 모드는 기본 범위에 포함하지 않는다. 그 요구가 확정되면 후보별 한국 환경 관측을 제공하는 별도 검증 경로가 필요하며, 같은 실행·후보에 대한 관측이 없거나 30분을 넘으면 보류하는 계약을 추가해야 한다. 현재 5곡 로컬 비교로 미래의 모든 후보를 검증했다고 간주하지 않는다.

### 기존 ID와 새 ID의 결정표

| 기존 ID | 새 ID | 결과 |
|---|---|---|
| 정상 | 정상 | 기존 유지 |
| 정상 | 이용 불가 또는 UNKNOWN | 기존 유지, 새 ID 보류/제외 |
| 이용 불가로 재확인 | 동일 곡이며 정상 | 근거가 포함된 대표 ID 변경 허용 |
| UNKNOWN | 정상 또는 UNKNOWN | 기존 연결 유지, 자동 전환 보류 |
| 없음 | 동일 곡이며 정상 | 신규 매칭 허용 |
| 없음 또는 이용 불가 | 검증된 대체 없음 | 해당 출력의 완전성을 확인할 수 없으면 게시 보류 |
| 어떤 상태든 | 다른 곡·다른 녹음 버전 | 대체 거절 |
| 수동 정책 존재 | 어떤 후보든 | 수동 정책 우선, 불가·불확실이면 자동 대체 대신 게시 보류 |

이 표는 일반 실행 정책이다. 이미 잘못 교체된 정상 ID를 되돌리는 `restore_unintended_chart_switch`는 §9의 사건 명세를 통과한 경우에만 별도로 허용한다. 새로운 정상 ID를 이용 불가라고 기록하여 이 예외를 우회하지 않는다.

## 4. 구현 단위와 파일 책임

| 구현 단위 | 주요 파일·경로 | 구현 내용 |
|---|---|---|
| 공통 재생 증거 | 제안: `ytmusic_playability.py` | DB 쓰기 없는 작은 결과 구조·상태 분류·조회 helper. client를 전달받으며 실행 내 중복 조회를 줄인다. |
| 읽기 전용 진단 | 제안: `diagnose_ytmusic_playability.py`, 별도 수동 Actions workflow | 기준곡·이전/현재 ID·실제 목록을 조회하고 정제된 결과만 출력한다. DB·게시 모듈의 import 부작용도 금지한다. |
| 대표 ID 정책 | `hype_db_store.py`의 canonical 선택, consolidate, 단일·일괄 저장, merge | 기존 보존과 검증된 복구를 같은 규칙으로 처리한다. 새 선택이 저장 과정에서 되돌아가지 않게 한다. |
| 중복 후처리 | `heal_split_tracks.py` | 최신 `updated_at` 기반 대표 ID 선택을 제거한다. 근거 없는 새 ID 승격은 보류하고 기존 안정적인 연결을 유지한다. |
| 캐시·검색 연결 | `ytmusic_to_ytmusic_crawl.py`, `crawler_common.py`, `ytmusic_playlist_sync.py` | 차트뿐 아니라 최종 선택된 캐시·검색·fallback 후보에도 판정을 적용한다. 잘못된 ID가 다른 소스를 통해 다시 들어오는지 검사한다. |
| 게시·출력 연결 | `sync_all.py`, `ytmusic_playlist_sync.py`, 각 소스 crawler, `hype_moment.py`, 필요 시 `hype_db_reports.py` | 필수 소스의 수집·매칭·후처리가 모두 끝난 뒤 공통 대표 ID 목록을 확정한다. 동일한 확정 상태로 게시·Hype·history를 만든다. |
| 검증·자동 실행 | 명시적으로 관리되는 오프라인 tests, `.github/workflows/daily-sync.yml` | 단위·통합 검사와 실환경 판정을 분리하고, 각각 실패하면 적절한 단계에서 멈춘다. |
| 사건 복구 | `repair_ytmusic_chart_incident.py`, `publish_chart_repair.py`, `hype_db_store.py`, 기존 게시 audit·reconcile | 읽기 전용 명세 생성, 변경 전 상태 대조, 제한된 DB 교정, 목록별 게시·재개·검증을 연결한다. 과거 사건 전용 스크립트의 날짜·ID를 그대로 재사용하지 않는다. |
| 과거 출력 교정 | `hype_db_reports.py`, `hype_moment.py`, `docs/index.html` | 명시한 영향 날짜만 교정하고 공유 metadata의 부수 변경을 막는다. 검증된 동일 곡의 ID 교체에서도 NEW·등락·추이를 연결한다. |

기존 helper·감사 기록·복구 절차를 우선 재사용한다. 범용 정책 엔진, 별도 외부 서비스, Redis, 새 테스트 프레임워크는 필요하지 않다. 새 모듈은 DB 없이 재사용 가능한 판정 부분을 분리하는 정도로 제한한다.

### 대표 ID 변경의 저장 계약

변경 결정에는 `run_id`, source ID, 기존 track UID, 예상 기존 대표 ID, 선택 ID, 변경 이유, 동일 곡 근거, 재생 관측 시각·환경을 담는다. 값의 이름은 구현 시 기존 구조에 맞춘다.

1. 네트워크 관측은 DB 쓰기 트랜잭션 밖에서 끝낸다.
2. 반영 직전에 현재 대표 ID·연결·수동 정책을 다시 읽는다.
3. 관측 당시 상태와 다르면 덮어쓰지 않고 재평가하거나 충돌로 보류한다.
4. 짧은 트랜잭션에서 대표 ID, 필요한 플랫폼 연결, alias, 정확한 영상의 metadata, 검증 표시와 색인을 함께 반영한다.
5. 영상 소유권 중복이 없어야 하고, 대표 ID가 있는 track은 canonical 플래그가 정확히 하나여야 한다.
6. 완료 처리 전에 저장 결과를 다시 조회하여 선택 결과·매칭 기록·게시 예정 목록과 일치시키고, 다르면 rollback한다.
7. 영구 metadata 캐시의 검증 표시를 실시간 재생 증거로 재사용하지 않는다.
8. 복구 helper는 호출자가 트랜잭션을 소유하며 내부 commit을 하지 않는다. 현재 `persist_crawl_run(conn=...)`은 내부에서 commit하므로 그대로 복구 트랜잭션에 넣지 않는다. 변경과 복구 영수증은 같은 트랜잭션에 기록한다.
9. `match_attempts` 등 과거 실행 결과를 복구된 ID로 다시 쓰지 않는다. 새로운 결정·복구 영수증이 실제 과거 시도를 참조하도록 한다.

### 실행 순서

`실행 환경 확인 → 필수 소스 전체 수집·매칭·동일성/재생 검증 → 필요한 identity 정리 → DB 반영·재조회 → 실행 전체의 대표 ID·출력 snapshot 확정 → 각 목록 게시·사후 관측 → 같은 snapshot의 Hype/history`

- 마지막 정상 snapshot을 검증 전에 stale로 만들거나 교체하지 않는다. 우선 메모리에서 후보를 만들고, 검증 통과 뒤 raw snapshot·선택 결과를 함께 확정한다. 실패 진단을 남기는 감사 기록은 정상 데이터와 구분한다.
- 개별 crawler의 게시를 뒤로 미루는 명시적 옵션을 추가하여 `sync_all.py`를 데이터 확정 단계와 게시 단계로 나눈다. 기존 `--db-only`는 Melon에서 매칭도 건너뛰므로 이름만 재사용하지 않는다. 새 옵션은 수집·매칭·검증·저장을 수행하고 외부 게시만 미루는 것으로 통일하며, 직접 실행의 기존 기본 동작은 보존한다.
- 후처리가 대표 ID를 바꾸는 일은 실행 전체의 최종 목록 확정 전에 끝낸다. 후처리 실패를 무조건 non-fatal로 넘기지 않는다. 뒤에 실행된 서비스가 앞서 게시한 목록의 canonical을 바꾸는 구조를 없앤다.
- 필수 소스의 정리가 끝나면 UID→대표 ID, source 순위, 게시 목록, Hype 입력을 하나의 확정 snapshot으로 잡는다. 각 게시·history 생성 직전에 현재 DB와 수동 정책이 이 snapshot과 일치하는지 확인하고, 달라지면 예전 결정으로 덮지 않는다.
- `data_ready`는 raw 수집 완료가 아니라 필요한 동일성·재생 선택·DB 정합성이 검증된 상태를 뜻하게 한다. `publish_ready`와 `publish_success`는 별개다.
- 인증·동일성·대표 ID 확정 실패가 있는 입력에 의존하는 Hype/history는 새 성공 snapshot으로 게시하지 않는다.
- 단순 외부 게시 실패와 데이터 검증 실패는 분리한다. 검증된 데이터의 history 게시를 허용하는 기존 정책은 보존할 수 있지만, 실제 플레이리스트 게시 성공으로 표시해서는 안 된다.
- DB commit 뒤 실제 목록에서 `isAvailable=false` 또는 재생 증거 충돌이 발견되면 단순 전달 실패로 취급하지 않는다. 해당 출력 snapshot의 검증 완료 자격을 취소하고 이에 의존하는 Hype/history의 새 게시를 막는다. 변경 직전 상태가 여전히 예상과 같을 때만 DB·외부 목록을 복구하며, 달라졌으면 명시적 복구 필요 상태로 남긴다.
- DB와 YouTube 사이에 하나의 원자적 트랜잭션이 있다고 가정하지 않는다. 기존 durable audit·소유권 기반 복구로 외부 실패를 처리한다.

## 5. 서브에이전트 구성과 병렬 운영

동시에 실행 가능한 구성은 주 담당 1명과 서브에이전트 최대 3명이다. 아래 7개 역할을 단계에 따라 교대한다. 별도 사용자 소유 작업을 만들 필요는 없다.

| 역할 | 담당 | 산출물 | 독립성·파일 경계 |
|---|---|---|---|
| 주 담당 | 판정 계약, 단계 통합, crawler·게시·workflow 연결, 출시 판단 | 결정표, 통합 diff, 체크포인트 기록, 실행 요약 | `ytmusic_playlist_sync.py`, crawler, `sync_all.py`, workflow의 공유 편집을 조정 |
| I1: API 구현 | 3상태 판정, 기준곡 확인, 실행 내 캐시·재시도, 읽기 전용 probe | 공통 helper, 정제 artifact 형식, API 측 테스트 | 새 판정·진단 파일 중심. 저장소·운영 DB는 변경하지 않음 |
| I2: 저장·복구 구현 | 대표 ID 정책, 단일·bulk 저장, alias·metadata 일관성, heal, 사건 명세·DB 교정 | 저장/후처리 수정, DB 재현 fixture, caller-owned 복구 트랜잭션 | `hype_db_store.py`, `heal_split_tracks.py`, 새 사건 복구 CLI 중심 |
| I3: history 구현 | 영향 날짜 교정, 공유 metadata 보존, 동일 곡 추이 연결 | scoped history patch, 전후 차이, 프런트 호환 변경 | `hype_db_reports.py`, `docs/index.html` 중심. `hype_moment.py`는 주 담당과 편집 순서 조정 |
| V1: 독립 로직 검증 | 실제 사례·반례로 판정과 보존/복구 정책 검증 | 실패 재현, 상태 결정표 대조, 독립 테스트·판정 | 구현자의 조건문을 그대로 복사하지 않고 입력·기대 결과·DB 재조회로 판단 |
| V2: 독립 운영 검증 | Actions, PostgreSQL, 게시·복구, Hype/history, 출시 전후 대조 | 실환경 관측, 실패 주입 결과, 전후 차이, GO/NO-GO 보고 | 구현 코드를 직접 고쳐 통과시키지 않음. 운영 변경은 주 담당이 확정한 범위로만 실행 |
| V3: 독립 이력·영향 검증 | 원본 기간·행, UID/서비스 연결, 전체 영향 날짜, 공유 metadata, NEW·등락·추이 | 독립 영향 목록, 날짜별 완전한 행·화면 대조, 누락/과잉 정정 판정 | I3의 기대값을 복사하지 않고 수정 전 artifact·raw 순위·서비스 ID에서 기대 결과 산출 |

권장 순서:

1. 설계·증거 수집: 주 담당 + I1 + I2 + V1.
2. 구현: 주 담당 + I1 + I2 + V1. 공통 반환 계약을 먼저 고정하고 파일 소유권을 나눈다.
3. 복구·history 구현: 주 담당 + I2 + I3 + V3. I1 완료 후 슬롯을 교대하며 V1의 반례를 인계한다.
4. 통합 검증: 주 담당 + V1 + V2 + V3. 발견된 결함은 검증자가 보고하고 해당 구현 담당이 다음 교대에서 수정한다.
5. 운영 검증: 주 담당 + V2 + V3 + 필요한 구현 담당 1명. V1의 저장·정책 판정을 포함한 세 검증 보고서로 다음 단계 진행 여부를 결정한다.

각 에이전트의 인계에는 변경 파일, 적용 규칙, 실행한 검사, 아직 확인하지 못한 사항, 반례를 포함한다. 테스트 수만으로 합격을 선언하지 않는다.

## 6. 단계별 로드맵과 체크포인트

| 단계 | 수행 내용 | 산출물 | 통과 기준·중단 조건 |
|---|---|---|---|
| P0 / CP0: 기준선 | 5곡·13곡 증거와 기존 코드/로컬 patch를 고정. 모든 canonical writer와 후처리 호출을 목록화. R0에서 영향 범위 확장 | 정제 fixture, 영향 경로 지도, 판정표, 재현 가능한 오프라인 실행 목록 | 제보 원인과 별도 Melon 게시 실패를 구분할 수 있어야 함. 추적되지 않은 테스트에 의존하면 기준선 미완료 |
| P1 / CP1: Actions 관측 | DB·게시와 분리된 읽기 전용 probe 구현. 동일 조건의 로컬·Actions 관측 비교 | 인증 유효성 결과, 기준곡 결과, 각 ID별 인증/공개 관측, 환경 차이 보고서 | 인증 확인과 정상 player 경로를 각각 입증해야 함. 둘 중 하나가 불확실하거나 설명 못한 환경 차이가 있으면 운영 연결을 막음 |
| P2 / CP2: 핵심 구현 | I1/I2 병렬 구현, 주 담당이 캐시·검색·후처리·게시 순서에 연결 | 판정 helper, 저장 계약, 공통 선택 정책, 후처리 수정 | 정상 기존 ID 무교체, UNKNOWN 무변경, 검증된 복구의 저장 후 유지. 수동 정책·다른 버전 보호 유지 |
| P3 / CP3: 통합 회귀 | 고정 입력을 기존/수정 코드와 분리 DB에 실행. SQLite·격리 PostgreSQL에서 중간 실패·재실행 검증. R2에서 오염 상태를 출발점으로 복구 시험 | 선택/DB/예정 목록/Hype/history 차이, 독립 검증 보고 | 의도 밖 ID 변화·순위 손실·중복·canonical 불일치 0. 실패한 run이 정상 snapshot을 밀어내면 NO-GO |
| P4 / CP4: 실제 환경 시험 | 쓰기 없는 Actions 비교 실행 후 격리 DB·시험 플레이리스트에서 실제 게시·복구 | 인증 정상 2회, 불확실 환경 1회, 게시/복원 관측 | 인증 실패 시 데이터·외부 목록 무변경. 실제 게시 결과와 정합성·availability 검사 통과. 로컬 통과만으로 대체 불가 |
| P5 / CP5: 운영 적용 | 예방 코드를 먼저 배포하고 R3의 운영 전제를 확인. R4 DB → R5 목록 → R6 history → R7 최종 검증을 순차 수행 | 적용 revision, 사건 명세, 백업·복구 영수증, 날짜별 정정 결과 | 계획 범위 밖 변화 0, 검증 불가 ID 신규 채택 0, 미해결/부분 완료를 성공으로 기록 0. R7 전까지 기존 오류 해소를 선언하지 않음 |
| P6 / CP6: 재발 확인 | 같은 주간 입력 재실행, 다음 일반 일일 경로와 실제 월요일 경로 관측 | 실행 간 ID 비교, 후처리 재실행 결과, 최종 보고 | 정상곡 ID의 이유 없는 왕복 변경 0, 실패 ID 재유입 0, DB·게시·최신 history 불일치 0 |

의존 관계는 `P0 → P1 → P2 → P3 → P4 → P5 → P6`이다. P1의 관측 코드와 P2의 순수 정책·fixture 작성은 병행할 수 있지만, 실제 재생 판정의 운영 기준은 CP1 통과 전 확정하지 않는다.

복구 경로는 `R0 → R1 → R2 → R3 → R4 → R5 → R6 → R7`이다. R0/R1의 증거 수집과 I3의 순수 파일 변환 구현은 예방 구현과 병행한다. R2는 CP3와 연결하고, R3는 CP4 통과와 예방 코드 배포를 요구한다. 운영 수정을 뒤로 미루기 위해 복구 구현·검증까지 생략해서는 안 된다. 세부 checkpoint는 §9.5에 둔다.

작업량 산정은 CP1 이후 실시한다. API 응답 차이, Actions 인증 상태, 격리 PostgreSQL·시험 목록 사용 가능 여부에 따라 일정이 달라지므로 현재 상태에서 고정 완료일을 약속하지 않는다. 실제 월요일 관측 대기와 순수 구현 시간을 구분한다.

2026-09-15 CP4 로컬 실측: 승인된 기존 비공개 시험 목록과 격리 SQLite에서 같은 6개 검사를 두 번 연속 통과했다. 정확히 같은 목록은 외부 쓰기 0회, 역순 변경은 소유권 토큰을 유지한 이동 4회, 한 곡 삭제·재추가와 원래 순서 복구도 통과했다. 실제 네트워크에서 비인증 계정은 게시 전에 차단됐고 목록과 SQLite 파일 바이트가 그대로였다. 앞선 이동 반영 지연은 원래 불확실 이력을 보존한 채 두 번의 정확한 관측과 명시적 전진 복구로 해결했으며 미완료 시험 audit는 0개다. 이는 로컬 시험 결과이며 Actions 관측과 운영 목록·사용자 재생의 완료 판정은 별도로 수행한다. 비공개 영수증은 사건별 보관 위치에 유지한다.

## 7. 검증 항목과 기대 결과

| 검증 사례 | 기대 결과 | 담당 |
|---|---|---|
| 제보 5곡의 당시 응답 재현 | 기존 정상 ID 유지. 신규 3개 불가 응답은 선택되지 않음 | V1 |
| 나머지 교체 8곡 | 실제 동일성·현재 상태를 별도 평가. 모두 불가라고 단정하지 않음 | V1/V2 |
| 공개 조회에서 정상곡도 Premium 제한 | 인증 결과를 대체하지 않음. 영구 불가 저장 없음 | V1 |
| 로그인 요구, challenge, 429, 5xx, timeout | UNKNOWN/환경 실패. 대량 재매칭·삭제·짧아진 목록 게시 없음 | V1/V2 |
| OK지만 오디오 정보 없음, `isAvailable` 누락·충돌 | 누락은 미관측. 충돌·필수 근거 부족은 UNKNOWN | V1 |
| 정상 기존 ID와 정상 새 앨범 ID | 기존 유지. 앨범 변경만으로 승격하지 않음 | V1 |
| 기존 불가, 검증된 동일 곡 대체 정상 | 선택→단일 저장→bulk 저장→재조회 뒤에도 새 ID 유지 | V1 |
| 차트 원본 ID 자체가 정상 canonical | 다른 ATV 관계만으로 교체하지 않음 | V1 |
| 신규 곡, 교차 서비스 캐시만 있는 곡 | 검증된 신규 매칭·기존 연결 재사용 가능. 중복 UID/곡 생성 없음 | V1 |
| source metadata가 자기 자신만 검증하는 오염 | 보호 대상으로 잘못 인정하지 않음. GRLS·Invitation 복구 성질 유지 | V1 |
| Live·remix·instrumental·다른 가수 | 같은 곡 대체로 혼합하지 않음 | V1 |
| 수동 block/split/override, 조회 중 수동 변경 | 최신 수동 정책 우선. 관측 전 상태로 덮지 않음 | V1/V2 |
| heal 최신 행이 불가 ID | 정상 대표 ID 유지. 최신 시각만으로 선택하지 않음 | V1 |
| 대표 ID 변경 후 metadata·색인·alias | 모두 실제 선택 영상과 일치. canonical 플래그 정확히 하나 | V1/V2 |
| 후보 목록의 정상 선택과 오염된 DB 대표 ID | 차이를 탐지하여 유효한 복구 또는 명시적 보류. 서로 다른 결과로 성공 처리하지 않음 | V1/V2 |
| API 완료 전·DB 반영 중·완료 표시 직전 실패 | 마지막 정상 snapshot 보존. 부분 반영이 completed로 남지 않음 | V2 |
| 후처리 실패 | 해당 입력이 Hype/history의 새 정상 입력으로 진행하지 않음 | V2 |
| 뒤에 실행되는 서비스가 같은 곡의 ID를 변경 | 전체 소스 정리 후 확정한 하나의 대표 ID snapshot을 모든 게시·Hype/history가 사용 | V2 |
| DB commit 후 availability 충돌 | 검증 완료 자격 취소, 관련 Hype/history 게시 중단, 최신 상태 대조 후 제한된 복구 | V2 |
| 플레이리스트 부분 추가, 응답 유실, 순서·개수 불일치 | durable 복구 적용. 임의 사용자 변경을 덮지 않음 | V2 |
| 두 번 동일 입력 실행 | 대표 ID·원본 ID·순위·플랫폼 연결 동일. 불필요한 외부 목록 변경 없음 | V1/V2 |
| 서로 다른 프로세스·실행 환경 | 만료된 재생 관측을 영구 캐시로 재사용하지 않음 | V2 |
| 오염된 DB에서 정상→정상 사건 복원 | REDRED/RUDE!의 이전 선택으로 명시적 복원이 가능하고 일반 재실행·heal은 이를 재교체하지 않음. 새 ID를 불가로 왜곡하지 않음 | V1 |
| 복구 선택과 단일/bulk 저장 | 선택 ID·canonical·정확한 metadata·새 영수증·예정 목록 일치. 과거 attempt는 당시 증거로 보존 | V1/V2 |
| 404의 목록은 이미 이전 ID, DB/history는 새 ID | 해당 목록 항목 제거·추가 0회. 확인된 나머지 출력만 교정 | V2 |
| 동일 곡 UID 병합 / 다른 곡의 잘못된 병합 | 전자는 현재 UID를 유지하는 최소 교정. 후자는 증명된 연결만 분리하고 증거 없으면 보류 | V1/V3 |
| old가 검색에 없지만 정확한 ID는 정상 | 검색 비노출을 불가로 취급하지 않음. 정확한 ID·곡 근거로 복원 가능 | V1 |
| 실패 run이 raw snapshot을 덮은 경우 | 검증된 백업에서 원본 행·순위·기간 복원. 자료 없으면 보류, completed 상태 조작 금지 | V2/V3 |
| DB commit 직후·목록 A/B 사이·history 게시 직후 강제 종료 | 같은 manifest와 durable 영수증으로 완료 단계 확인 후 재개. 중복 추가·순위 손실·거짓 완료 0 | V2 |
| 목록 A 적용 뒤 재생 증거 만료 | 고정 명세에 새 재검증 영수증을 연결하여 B부터 재개. DB·A 중복 적용 0, 근거 없는 expiry 연장 0 | V2 |
| 복구 전 백업에 이미 불가 ID가 포함 | 오염 백업으로 돌아간 것을 정상 복구로 기록하지 않음. 안전한 전진 복구 또는 명시적 보류 | V2 |
| 이전 날짜 오류 + 최신 날짜 정상 | 지정한 과거 날짜 오류도 교정. 최신 날짜만 갱신하고 완료 처리하지 않음 | V3 |
| 순수 ID 교정 / 잘못된 곡 병합 교정 | 전자는 점수·순위 유지가 기본. 후자는 동일 raw 입력으로 전체 영향 날짜의 합산·중복 제거·간접 등락을 독립 재계산 | V3 |
| 정당한 ID 변경, NEW·등락·추이 | 검증된 같은 곡은 연결되고 다른 녹음·동명이곡은 연결되지 않음. 다음 표시 날짜의 파급 영향까지 검증 | V3 |
| 복구 뒤 일반 export와 다음 날짜 생성 | 재계산 결과에도 검증된 identity_key 계승. serializer 보존만 통과하고 다음 실행에 키가 사라지는 경우 불합격 | V3 |
| 공유 track metadata 변경 | 펼친 날짜별 행·hydrate 결과를 비교. 영향 밖의 화면 필드 변화 0, 의도한 정정만 manifest에 기록 | V3 |
| 날짜별 metadata override·명시적 빈 문자열 | 허용된 표시 필드만 적용, video_id 재정의 금지. compact→inflate→compact와 실제 JS hydrate에서 같은 행/표시 유지 | V3 |
| 일간 D−1·월요일·연말 ISO 주차·수집 지연/누락 | 각 날짜가 사용한 원본 기간을 명세와 대조. 미래 자료 혼입·임의 재수집으로 과거 순위 변경 0 | V3 |
| 로컬 history 파일과 공개 페이지 불일치 | 생성·저장소 반영·공개 반영을 별도 상태로 기록. 공개 내용 확인 전 게시 완료 금지 | V2/V3 |
| API 정상인데 사용자 앱에서 실제 실패 | API 검증 완료와 실제 재생 문제 해결을 구분하고 환경 차이를 미해결 항목으로 유지 | V2 |

순수 DB no-op fixture에서는 바이트 불변을 검사한다. 실제 실행에서는 감사 시각·run 기록이 늘 수 있으므로 업무 데이터의 불변성과 외부 변경 횟수를 검사한다.

오프라인 검사는 네트워크·운영 PostgreSQL 연결을 차단한다. PostgreSQL 통합 검사는 별도 시험 DB 또는 명시적으로 격리된 schema에서만 수행하고, 시작 전에 연결 대상·현재 schema가 시험용인지 검증한다. `--db-only`는 쓰기 모드이므로 읽기 전용 진단에 사용하지 않는다. `--dry-run` 역시 실제 DB·파일·게시 함수 호출이 없는지 검사해야 한다.

현재 202개 기존 테스트 상당수가 로컬에만 존재한다. 릴리스 필수 검사는 `.gitignore` 예외와 명시적인 실행 목록으로 재현 가능하게 포함하고, 실제 운영을 수행하는 opt-in 스크립트를 무차별 discovery에 섞지 않는다. 깨끗한 checkout에서 요구 검사를 다시 실행한다.

## 8. Actions 진단·로그·시간 예산

읽기 전용 진단은 수동 실행 workflow로 시작한다. `contents: read`, 운영 DB 자격증명 없음, 인증 client의 읽기 API만 허용한다. 인증 파일은 runner 임시 경로에 만들고 마지막 정리 단계에서 제거한다. 파일·게시·DB 변경은 dry-run 문자열이 아니라 실제 호출 금지 검사로 확인한다.

artifact의 허용 필드:

- run ID, code revision, Python·ytmusicapi 버전, UTC 확인 시각, 실행 환경 구분과 언어 설정.
- source ID, 기존/새 video ID, 제목·가수·앨범, 정확한 ID 일치 여부.
- 정규화한 player 상태·사유 코드, 오디오 정보 존재 여부, 정확한 ID의 `isAvailable` 관측값.
- 기준곡 결과, 최종 3상태 판정, 선택된 ID와 보존/교체/보류 이유.

쿠키·헤더·토큰·인증 파일·전체 player 응답·서명 URL·원시 IP·상세 예외 객체는 기록하지 않는다. 정제 JSON만 지정해서 업로드하고, 초기 보존 기간은 7일로 제한한다. 비밀 패턴 검사 실패 시 artifact 업로드를 막는다.

호출 예산의 초기값:

- 진단은 기준곡 3개와 제보 10개 ID, 실제 목록 1회부터 시작한다. 겹치는 ID는 중복 조회하지 않는다.
- 운영은 변경 후보와 최종 선택 목록의 ID를 검증한다. 실행 내 memo를 재사용하고 새로운 영구 재생 캐시는 만들지 않는다.
- 요청당 timeout 20초, 일시 오류 재시도 최대 2회. `Retry-After`를 존중하되 전체 시간 예산을 넘기지 않는다. 인증 거절·challenge는 반복 요청하지 않는다.
- 명확한 곡별 이용 불가는 해당 ID만 1회 재확인한다.
- 기본은 순차 호출이다. 병렬화가 필요하면 client 공유 안전성을 확인하고 독립 client 최대 2개부터 검증한다.
- 초기 시간 상한은 진단 5분, 운영 재생 검증 10분이다. 초과는 UNKNOWN/보류이며 성공으로 완화하지 않는다. 실제 처리량은 CP1에서 측정하여 전체 Actions 제한 내에 들어오는지 확인한다.
- 로컬·Actions 비교는 같은 code revision·라이브러리 버전·설정으로 가급적 30분 안에 실시한다. 관측 시점과 환경이 다른 결과를 합쳐 하나의 사실로 저장하지 않는다.

## 9. 운영 반영과 기존 데이터 복구

### 9.1 복구 대상의 확정

이번 13곡은 조사 시작점이며 복구 대상 수의 상한이 아니다. 9/13 게시 revision `44bd667`, 9/14 `5286274`, 해당 Actions 실행, 현재 운영 DB·실제 목록을 대조한다. 로컬 DB 사본이나 과거 probe를 현재 운영 상태로 간주하지 않는다. 복구를 다음 날 실행하더라도 사건 날짜를 실행 당일로 바꾸지 않는다.

이전/신규 video ID → 소유 UID·alias → 원본 서비스 ID → 관련 job/variant/기간 → 실제 목록·Hype·history 날짜를 추적한다. Apple 4개, Spotify 2개, Melon 일간·주간·Gen-Z variant, YouTube 주간을 현재 job 설정과 대조하여 빠진 경로가 없는지 확인한다. 사용 기간이 다음 수집일까지 이어지는 날짜, Hype 합산·중복 제거로 순위가 움직인 다른 곡, 다음 표시 날짜의 NEW·등락, 동일 곡의 추이까지 영향 목록에 포함한다.

각 `곡 × 출력/날짜`를 `수정 필요`, `정상 확인`, `증거 부족`으로 분류한다. 13곡 외에 같은 원인으로 오염된 항목은 근거를 붙여 확장한다. 다른 원인의 과거 오류가 발견되면 별도 case로 원인·범위·복구 조건을 정하며 이번 복원이 자동으로 해결한다고 주장하지 않는다.

### 9.2 사례별 복구 결정

아래 재생 상태는 **저장된 9/14 로컬 인증 관측**이다. 운영 반영 직전에 정확한 ID, 동일 녹음/버전, 실행 환경과 최신 수동 정책을 다시 확인해야 한다.

| 곡 | 이전 ID → 교체된 ID | 당시 근거 | 복구 계획 |
|---|---|---|---|
| REDRED | `5sOgJQ3N03Q` → `DBOyG7y_FQg` | 둘 다 OK. REDRED → GREENGREEN 앨범 변화 | `restore_unintended_chart_switch`: 의도하지 않은 자동 교체 이력과 이전 곡의 동일성·현재 정상 상태를 확인하고 이전 선택 복원. 새 ID를 불가로 기록하지 않음 |
| RUDE! | `Q4AE3ub4nBM` → `EsU7ng0KIy0` | 둘 다 OK. RUDE! → Lemon Tang 앨범 변화 | REDRED와 같은 정책 복원. 제목·가수 또는 앨범 차이만으로 같은 녹음이라고 확정하지 않음 |
| Pop Off Pop Off | `QbsbqekMkCU` → `cQPXraDIvS4` | 이전 OK, 새 UNPLAYABLE. 실제 Weekly 새 항목 unavailable | `replace_unavailable_recording`: 현재 정상 old와 새 ID의 상태를 재확인한 뒤 오염된 출력 복원 |
| 404 (New Era) | `MUny_GDYIDM` → `KL7km3PxilQ` | 이전 OK, 새 UNPLAYABLE. 실제 Weekly는 이미 old/available, history는 new | DB 현재값 별도 확인. 불일치 DB·history·다른 목록만 교정하며 이미 정상인 Weekly의 이 항목에는 추가/제거 0회 |
| Less than a Lover | `z9ifbheDGFM` → `1PVUTUFxq8g` | 이전 OK, 새 UNPLAYABLE. 실제 Weekly 새 항목 unavailable | Pop Off와 같은 이용 불가 복구 |
| BANG BANG | `TfAiFxoXVlE` → `1moSPsMlDBA` | 교체 이력 확인 | 동일성·상태 조사 후 복원/유지/보류 개별 확정 |
| FALLEN ANGEL | `cOARkf5ZmtI` → `SCl9CL9vqa4` | 교체 이력 확인 | 동일성·상태 조사 후 개별 확정 |
| ICONIC HEART | `A9EpZWrQ3dM` → `hl7_dxuytRQ` | 교체 이력 확인 | 동일성·상태 조사 후 개별 확정 |
| Candy Pink Magic Hole Flip Phone | `DbI-M_jZukE` → `QJmPjIwxzhc` | 교체 이력 확인 | 동일성·상태 조사 후 개별 확정 |
| Animal | `vOfyxPRunFM` → `kDsRsv3DsHg` | 교체 이력 확인 | 동일성·상태 조사 후 개별 확정 |
| Bloody Paradise | `EmRFwlGlbb8` → `YJhEsDa2o8w` | 교체 이력 확인 | 동일성·상태 조사 후 개별 확정 |
| Heroine | `XNc6BJlvczs` → `107UoVP2fAA` | 차트 교체 외 heal 병합 이력 있음 | 현재 UID·옛 UID의 연결을 확인. 같은 곡 병합이면 현재 UID 유지와 canonical 교정을 우선 |
| 꿈꾸던 어른이 되었나요? | `GxChUrrY4bc` → `JJx_WQXOeK0` | source ID 자체가 이전 canonical이었던 사례 | source=canonical인 경로까지 복원 후 재실행·heal 검증 |

정상→정상 복원에는 원인 실행, 변경 전 대표였다는 증거, 현재 정확한 ID의 정상 여부, 동일 곡 근거, 최신 수동 정책과의 무충돌, 명시한 변경 범위가 모두 필요하다. `replace_unavailable_recording`과 별도 사유로 기록한다. 이전 관측과 달리 현재 불확실하면 `unknown`으로 보류한다. 검색 비노출만으로 old를 폐기하지 않으며, 곡을 삭제해 목록을 줄이는 것으로 복구 완료를 만들지 않는다.

당시 불가였던 새 ID가 지금 정상이라면 이용 불가 사유를 재사용하지 않는다. 의도하지 않은 교체 증거가 충분하면 정상→정상 사건 복원으로 재분류하고, 이전 ID가 현재 불가라면 원래 ID 복원 대신 검증된 현재 선택/대안을 평가한다.

### 9.3 복구 명세와 실행 인터페이스

구현된 DB CLI `repair_ytmusic_chart_incident.py`는 `plan → rehearse → apply-db → verify`와 같은 명세의 `resume`을 제공한다. 외부 출력 CLI `publish_chart_repair.py`는 `publish → install-history → verify-public-history`를 분리한다. rollback은 caller transaction의 실제 롤백 및 검증된 보상 범위로 제한하며, 임의로 전체 백업을 운영에 덮어쓰는 명령을 제공하지 않는다. 기존 9/7 복구 스크립트에는 고정 날짜·ID·정책이 있으므로 그대로 실행하지 않는다.

`plan`은 읽기 전용 연결과 파일 사본만 사용한다. DB 초기화/자동 교정이나 게시 모듈 import 부작용 없이 다음을 정제 JSON으로 만든다.

| 명세 영역 | 필수 내용 |
|---|---|
| 식별·버전 | repair ID, case ID, 원인 run/revision, 구현 revision·라이브러리 버전, 명세 해시, 생성/관측 UTC 시각 |
| 교정 근거 | source service/song ID·URL, 당시 old/new ID, 동일 녹음·앨범 근거, 복구 사유, 근거 artifact 위치·해시 |
| DB 전제 | 현재 UID/canonical, old/new 영상의 소유 UID, 영향 플랫폼 연결·alias, 수동 정책, 변경 대상 행의 before-image와 fingerprint |
| 원하는 결과 | 복구 UID/ID, 정확한 영상 metadata·검증 표시, 허용된 연결 변경, 정상 관계/수동 정책 보존 조건 |
| 재생 증거 | 지정 계정/환경의 인증·기준곡 상태, 정확한 대상별 판정, 관측 시각, expiry, 상충하는 목록 관측 |
| 출력 범위 | 실제 playlist ID/job/variant, 원본 기간·순서, 영향 history 날짜, 최신 게시용 snapshot, 날짜별 허용 ID/metadata/점수/순위 변화 |
| 복원·재개 | DB before-image, 실제 목록의 순서·videoId·setVideoId·소유권 증거, history 원본 해시·Git revision, 단계별 audit 참조 |

재생 증거는 초기 최대 30분을 기준으로 하되 CP1의 처리량에 맞춰 명시적으로 확정한다. 만료 시 고정 manifest를 편집하지 않고 새 관측을 재검증 영수증으로 연결한다. 대상·결정·전제가 그대로 유효하면 그 영수증을 근거로 남은 단계부터 재개한다. 계정/환경/코드·수동 정책 또는 결정/대상이 달라지면 기존 명세와 완료 단계를 참조하는 새 명세로 재계획한다. 오래된 판정을 살리려고 expiry만 연장하지 않는다. 공개 artifact에는 §8 허용 필드만 넣고 복구에 필요한 비공개 before-image는 접근을 제한한 보관 위치로 분리한다. 진단 artifact의 7일 만료와 별개로 복구 manifest·영수증·백업은 사건 종료와 후속 검증까지 유지한다.

DB 변경과 적용 영수증은 같은 트랜잭션에 기록한다. 우선 기존 `migration_reports.payload_json`에 명세 해시·case별 상태·before/after fingerprint·출력 audit 참조를 넣어 재사용한다. DB commit 직후 프로세스가 종료되어도 적용 여부를 확인할 수 있어야 한다. 외부 게시 진행은 기존 `playlist_update_runs`/items와 durable evidence에 연결하고, 로컬 JSON만을 유일한 진행 기록으로 삼지 않는다.

### 9.4 테이블별 복구와 병합 처리

| 대상 | 허용하는 교정·보존 계약 |
|---|---|
| `tracks` | 같은 곡이라면 현재 UID를 유지하면서 대표 ID·정확한 영상 metadata·검증 표시 교정. 옛 UID를 되살리는 것이 복구의 필수 조건은 아님 |
| `platform_song_ids` | 근거가 있는 서비스 연결만 교정. 합쳐진 UID의 연결 전체를 제목만 보고 이동하지 않음 |
| `yt_video_ids` | 영상 소유권을 검증하고 canonical 플래그 하나 보장. 같은 곡의 새 ID는 alias로 남길 수 있으며 alias 존재를 재생 가능 증거로 쓰지 않음 |
| `track_list` | 서비스 원본 metadata 보존. 잘못 복사된 값이 확인된 경우에만 해당 서비스의 근거로 교정 |
| `metadata_lookup_index` | 영향 UID의 검증된 metadata에서 제한적으로 재구축. 무관한 UID의 키를 빼앗거나 전체 캐시를 삭제하지 않음 |
| `ytmusic_song_translations` | 잘못된 영상에 귀속된 번역만 무효화·재검증. 정상 아티스트 번역 캐시 보존 |
| `source_song_relations` | 실제 관측 관계 유지. canonical로 잘못 승격된 관계를 삭제하여 원인을 숨기지 않으며 공통 정책으로 재승격 차단 |
| `match_runs`, `match_attempts`, `review_conflicts` | 과거 실행 ID·성공/실패를 다시 쓰지 않고 새 교정 영수증에서 참조. 새 실행의 저장·선택 불일치를 completed로 기록하지 않음 |
| `playlist_order`, `chart_source_audit` | 정상 raw 곡 ID·순위·기간 보존. 실패 실행이 덮어쓴 원본이 확인되면 정확한 백업/수집 artifact로 별도 복원하고 새 검증 결과 생성 |
| `manual_overrides` | 현재 block/split/override 보존. 병합 과정에서 바뀐 UID 참조를 과거 정책 증거 없이 역변경하지 않음 |
| 게시 audit·history | 대상별 새 복구 영수증과 명시한 날짜의 정정 기록을 추가. 당시 잘못 게시했다는 증거는 별도 보존 |

`_merge_track_uids`는 연결·alias·과거 attempt UID·일부 수동 참조를 옮기고 loser track을 삭제한다. 따라서 복구는 다음처럼 나눈다.

- **같은 곡의 병합:** 현재 UID를 유지하는 최소 교정을 우선한다. 이전 정상 video ID로 canonical을 복구해도 된다.
- **서로 다른 곡의 오병합:** 원래 서비스 연결·버전·수동 split·before-image로 증명한 관계만 재구성한다. 연결된 여러 행은 한 묶음으로 트랜잭션 처리하고 소유권/색인 충돌을 검사한다.
- **정확한 과거 복원 근거 없음:** 현재 상태를 검증된 정상 연결로 교정할 수 있는지 별도로 판단한다. 가능하더라도 삭제된 원래 UID까지 정확히 복원했다고 표현하지 않는다. 연결 자체를 증명할 수 없으면 `identity_review_required`로 보류한다.

일반 `upsert`/bulk에 old를 다시 넣는 것만으로 끝내지 않는다. caller-owned 트랜잭션에서 예상 현재값을 비교하고 지정 행만 바꾼 뒤, commit 전후에 canonical·alias·metadata·새 영수증·게시 예정 ID를 재조회한다. 불일치하면 commit 전에 rollback하며, 무관한 업무 행의 fingerprint가 달라지면 실패다. 영향 밖 감사 시각까지 바이트 불변을 요구하는 검사와 업무 데이터 불변 검사는 구분한다.

### 9.5 복구 단계·담당·체크포인트

| 단계 | 수행·담당 | 산출물과 통과 기준 |
|---|---|---|
| R0: 영향 조사 | 주 담당·V1·V3. 13곡에서 모든 연결/출력/날짜로 확장, 현재 DB·목록 독립 확인, 증거 보존 범위 확인 | case 목록과 `수정 필요/정상/증거 부족` 판정. 백업 존재만으로 사고 직전 상태 복원 가능이라 단정하지 않음 |
| R1: 명세·미리보기 | I2·I3 작성, V1·V3 독립 대조. 고정 raw 입력에서 DB·목록·history의 전후 결과 생성 | 해시가 고정된 manifest, 변경 행·날짜·간접 순위 변화, no-op 대상, 미해결 이유. 누락·근거 없는 광역 변경이면 중단 |
| R2: 격리 복구 시험 | I2·I3 구현, V1·V2·V3 검증. 오염 fixture와 시험 DB, 사본 history, 시험 목록 사용 | SQLite·격리 PostgreSQL의 rollback/commit/재개, 단일/bulk 저장, 모든 실패 지점과 화면 회귀 통과. 운영 자료 직접 쓰기 없음 |
| R3: 운영 준비 | 주 담당·V2. CP4 및 예방 코드 배포 확인, 정상 동기화와 복구의 동시 쓰기 차단, 미완료 게시 audit 확인, 현재 before-image 확보·재생 근거 갱신 | 대상·revision·수동 정책·소유권·backup freshness 대조. 변경 후 예방 코드와 history reader가 호환돼야 함. 조건 변경 시 명세 재생성 |
| R4: DB 교정 | 주 담당 실행, V1/V2 재조회. 네트워크 밖에서 짧은 트랜잭션으로 관련 행과 영수증 적용 | 원하는 canonical/metadata/연결/색인 일치, raw 순위·수동 정책 보존. commit 직후 종료해도 영수증으로 상태 판별 |
| R5: 목록 교정 | 주 담당·V2. 동일하게 확정된 최신 출력 snapshot으로 영향 목록별 적용·실제 재조회 | 전체 개수·순서·ID·관련 availability·소유권 확인. 이미 정상인 항목을 불필요하게 제거/추가하지 않음. 한 목록 성공을 전체 성공으로 기록하지 않음 |
| R6: history 정정·게시 | I3 미리보기의 동일 입력을 주 담당이 반영, V3 내용·V2 게시 확인 | 지정 날짜와 표시 의존 범위만 교정, 영향 밖 화면 변화 0. 파일→저장소 revision→공개 JSON/화면을 구분해 확인 |
| R7: 사건 마감 | V1/V2/V3 독립 보고 후 주 담당 판정. 제보 5곡·나머지 8곡·확장 대상 전수 대조, P6 재실행 및 실제 사용 환경 수용 검사 | 데이터 교정·API/목록 검증·사용자 재생 확인을 각각 기록. 보류·부분 완료·관련 별도 실패가 남으면 전체 해소 선언 금지 |

R3 준비부터 R4~R6의 부분 적용 동안 정상 동기화가 중간 복구 상태를 새 정상 상태로 발행하지 못해야 한다. Actions concurrency와 기존 잠금/활성 복구 상태 확인을 이용해 쓰기 주체를 하나로 제한하고, 로컬 실행도 같은 보호를 확인한다. 잠금만 믿지 않고 각 단계에서 행·목록 fingerprint를 다시 확인한다. 외부 API 대기 중 DB 쓰기 트랜잭션을 오래 유지하지 않는다.

R7에서 필수 DB·목록·공개 history의 정합성과 재생 증거를 확인하면 쓰기 차단을 해제하고 사건 상태는 `검증 대기`로 전환한다. 그 뒤 예방 코드로 동일 입력·일일·월요일 실행을 허용해 P6를 검증한다. 사용자 재생 확인이나 월요일을 기다리는 동안 정상 동기화를 계속 잠가 두지 않는다. 필수 출력이 아직 부분 적용 상태라면 해제하지 않고 재개·보상 대상을 명시한다.

복구용 raw snapshot은 당시 오류를 정정하기 위한 입력으로 고정한다. 현재 실제 목록은 현재 게시 대상 기간의 검증된 raw snapshot으로 재구성한다. 과거 날짜 복구를 이유로 현재 목록에 어제 또는 지난 주 순위를 게시하지 않는다. 재수집으로 과거 원본 순위를 바꾸는 방식도 사용하지 않는다.

### 9.6 history·Hype·화면의 한정 정정

기존 계획의 “최신 history만 갱신하고 과거 ranking은 전부 보존”을 다음 계약으로 대체한다.

1. **정정 전 증거 보존:** 현재 및 사고 전 Git의 history, 관련 raw 행·기간·순위, 원본 해시를 보존한다. 당시 게시물을 보관하는 것과 사용자가 보는 오류를 교정하는 것은 별개다.
2. **명시한 날짜만 계산:** R1의 `affected_dates`와 그 표시 의존 범위를 사용한다. 기본 exporter는 기존 파일이 있으면 최신 1일만 갱신하고, 새 출력 경로이거나 `full_rebuild`이면 더 넓게 재계산한다. `hype_moment --history-date`도 전체 history 재생성을 켜므로 그대로 복구에 쓰지 않는다. 기존 payload를 읽어 대상 날짜만 교체하는 명시적 helper를 구현한다.
3. **날짜별 입력 고정:** 현재 보고서는 과거 raw 행을 현재 `platform_song_ids`/canonical과 join한다. 사고 당시 그대로의 재현이 필요하면 당시 매핑 artifact를 사용하고, 자료가 없으면 수정된 연결을 적용한 역사 교정이라고 기록한다. 영향 밖 과거 매핑을 현재 canonical로 일괄 치환하지 않는다. 현재 구현에 이미 있는 월요일 주차 제한은 유지하며 일간 D−1·ISO 연말·수집 지연/누락을 별도 검증한다.
4. **원본과 파생 순위 분리:** raw 서비스 순위·기간은 보존한다. 순수 ID 교정이면 같은 곡의 점수·순위 유지가 기본이다. 오병합/누락 교정으로 합산·중복 제거·동점 정렬이 달라지면 영향 날짜 전체 결과를 재계산하고 다른 곡의 순위 이동까지 사유와 기대값에 포함한다. 대상 13행만 비교하여 합격하지 않는다.
5. **공유 metadata의 부수 변경 차단:** `inflate_frontend_history`로 날짜별 완전한 행을 펼쳐 전후 비교한다. 공통 `tracks[video_id]`의 최신 metadata가 다른 날짜를 바꾸지 않도록, 공통 값과 다른 날짜별 metadata는 그 날짜의 ranking row에 필요한 필드만 보존한다. 제목·가수·앨범·표시/서비스 링크 등 허용 필드만 덮을 수 있고 `video_id` 재정의는 금지한다. override 없음과 명시적 빈 문자열을 구분하여 과거의 빈 값도 보존한다. 현재 Python inflate와 JS hydrate의 ranking 우선 병합 방식을 활용하고 compact가 이 값을 다시 버리지 않게 수정한다. validator는 공통 tracks뿐 아니라 펼친 정정 날짜의 실제 행을 검사한다. compact→inflate→compact 반복과 실제 JS hydrate에서 제목·가수·앨범·링크·점수·순위·표시가 같은지 확인한다.
6. **NEW·등락·추이 연결:** 재생용 `video_id`와 비교용 동일 곡 키를 분리한다. 제안하는 선택 필드는 ranking row의 `identity_key`이며, 근거 있는 원본 서비스 연결과 검증된 교체/병합 기록으로 이전 날짜의 키를 이어받는다. 키가 없는 기존 자료는 video ID로 처리한다. 단순 현재 UID, 제목·가수만으로 옛 서로 다른 곡을 연결하지 않는다. 일반 날짜 재계산·신규 날짜 생성 단계도 검증된 기존 키를 계승하고 serializer가 보존하며, 프런트의 NEW·등락·추이 조회가 같은 비교 규칙을 사용하도록 한다. 키의 기원이 되는 명세/교체 영수증을 유지한다. 복구→일반 export→다음 날짜 export에서 연속성이 유지되는지 검사한다.
7. **표시 의존 범위까지 검증:** 영향 날짜 다음의 실제 표시 날짜, 해당 곡의 전체 추이 기간을 확인하고 표시 차이의 영향 범위를 닫는다. 현재 Python 보고서는 `previous_apple_videos`를 전달받지만 점수에 사용하지 않고 `is_new_wave`도 생성하지 않는다. 이번 변경은 NEW·등락·추이의 곡 연결을 고치며 신규 진입 보너스나 새 점수 정책을 도입하지 않는다. 점수·순위 교정은 4항의 잘못된 연결·합산·중복 제거 등 확인된 원인에 한정한다. 동일성을 입증할 수 없으면 억지로 추이를 이어 붙이지 않고 미해결로 기록한다.
8. **영향 밖 보존:** 명세 밖 날짜·곡의 완전한 업무/화면 필드와 순서는 불변이어야 한다. 새 보조 필드·생성 시각과 같은 의도한 형식 변경은 별도로 대조한다. 현재 31일 보존과 보관된 과거 artifact의 실제 범위를 조사하며 복구 실행 중 정상 날짜를 추가 만료시키지 않는다.
9. **게시까지 확인:** 검증된 임시 파일을 원자적으로 교체한 뒤 저장소 반영과 공개 게시를 기존 경로로 수행한다. Git head나 파일 해시가 미리보기 이후 바뀌었으면 덮지 않고 최신 원본에서 재계획한다. GitHub Pages가 제공하는 JSON 내용과 실제 hydrate 결과가 원하는 revision의 결과와 일치해야 공개 복구 완료다. 구형/확장 schema 2 입력의 호환성을 검증하고 필요한 reader 변경을 먼저 반영한다.

과거 화면 metadata는 이미 공유 값 때문에 바뀌었을 수 있다. 검증된 이전 게시 artifact가 있는 범위는 복원하고, 없는 범위는 정확한 당시 표시 복원을 보장하지 않는다. 영향을 받은 과거 출력의 정정은 이번 복구 범위에 포함하지만, 무관한 날짜의 전체 재생성은 포함하지 않는다.

### 9.7 부분 실패·재개·보상

DB, 여러 플레이리스트, Git, 공개 페이지를 한 트랜잭션으로 묶을 수 없다. 각 단계의 `pending/applied/verified/blocked`를 durable 영수증으로 관리하고 관측한 실제 상태를 우선한다.

| 실패 시점 | 처리 계약 |
|---|---|
| DB commit 전 | 트랜잭션 rollback. 현재 데이터는 유지하고 실패 근거만 구분해 기록 |
| DB commit 직후 응답/프로세스 유실 | 동일 트랜잭션의 영수증과 현재 행을 읽어 적용 여부 확인. 복구를 중복 적용하지 않음 |
| 목록 A 성공, B 실패 | 유효한 복구라면 B부터 재개. A를 자동으로 사고 상태로 되돌리지 않으며 전체 완료와 관련 history 성공은 보류 |
| 목록 쓰기는 성공, 응답만 유실 | 실제 ID·순서·소유권을 읽고 기존 audit과 대조한 뒤 다음 단계로 진행. 무조건 재추가 금지 |
| 사용자/다른 실행이 DB·목록 변경 | 명세의 예상 전후 상태와 일치하지 않으면 보류. 소유권을 입증할 수 없는 항목을 삭제하거나 덮지 않음 |
| 게시 후 availability false/상충 증거 | 후보의 검증 자격 취소. 관련 이후 게시·history 중단, 이미 반영한 DB/목록도 영향 범위에 포함하여 재평가 |
| history 파일만 생성 또는 저장소 반영만 성공 | 단계별 상태 유지. 공개 내용 조회로 실제 완료 여부를 확인하며 같은 파일을 중복 게시하지 않음 |
| 보상도 실패 | `recovery_required`로 남기고 자동 실행이 성공으로 덮지 못하게 함. 미해결 범위·현재 상태·재개 조건을 기록 |

복구 직전 백업 자체가 불가 ID를 포함한 사고 상태일 수 있다. 따라서 백업은 증거와 제한된 되돌리기 자료이며 자동으로 안전한 목표가 되지 않는다. 교정 결정이 유효하면 미완료 출력을 정상 상태로 마치는 방식을 우선한다. rollback 대상도 현재 정상·소유권·수동 정책을 검증해야 한다. 코드 rollback만으로 데이터가 복구됐다고 주장하거나, 원인 정책이 있는 예전 코드를 다시 활성화하지 않는다.

### 9.8 자동으로 해소할 수 없는 조건과 별도 실패

| 조건 | 추가 해소 계획·마감 방식 |
|---|---|
| 이전 ID가 현재 불가이거나 old/new 모두 UNKNOWN | 정상 환경 재확인, 정확한 ID 직접 조회, 동일 녹음의 검증된 대안 탐색. 근거가 생기기 전 전환·완료 보류. 삭제된 원본 자체를 복구할 수 있다고 약속하지 않음 |
| 서로 다른 곡의 오병합인데 원래 연결 증거 없음 | 사고 전 DB 백업·원본 서비스 artifact·병합 로그에서 소유권 복구. 부족한 case는 `identity_review_required`; 제목만으로 분리하지 않음 |
| raw snapshot이 오염됐고 정확한 옛 순위 자료 없음 | 보관된 수집 artifact·검증 가능한 해당 기간 자료 확인. 확보하지 못하면 그 기간의 정확한 역사 교정은 미해결로 남김 |
| 현재 수동 정책·사용자 목록이 복구 명세와 충돌 | 최신 변경을 보존하고 해당 case 재계획. 다른 검증된 독립 case의 상태와 분리 |
| Actions는 정상, 실제 사용자 계정/지역/앱에서는 실패 | 로컬과 Actions의 같은 ID/시점 관측을 대조하고 환경 불일치로 분류. 실제 사용 목록에서 재생 진행·오디오 확인 전 사용자 문제 해소로 표현하지 않음 |
| 별도 Melon Top-100-Daily 게시 실패 | 이번 ID 원인과 분리해 현재 실제 목록·기존 복원 audit 확인. 자료 수집 완료와 게시 완료를 구분하고 기존 durable reconcile을 시험한 뒤 필요한 게시만 복구 |

Melon 실패는 당시 빈 목록 관측과 trackCount 불일치 후 이전 목록을 복원한 게시 문제다. 재생 판정 코드를 바꿔도 자동으로 해결되지는 않는다. 현재 정상 여부를 읽어 case를 닫거나 별도 게시 복구가 필요함을 명시한다. 이 문제가 남아 있으면 “기존 오류 전체 해소”로 마감하지 않는다.

## 10. 완료 판정

주 담당은 V1·V2·V3의 독립 보고서를 기준으로 예방 구현 완료, 기존 데이터 교정 완료, API/목록 검증 완료, 실제 사용자 재생 확인을 구분하여 선언한다. 계획서 갱신이나 예방 코드 배포만으로 기존 오류 복구 완료를 선언하지 않는다.

- 동일성·재생 판정표, 모든 대표 ID writer의 적용 확인, 오프라인 및 격리 PostgreSQL 결과.
- Actions의 실제 인증 정상 2회와 불확실 실행 1회에 대한 읽기 전용 관측.
- 전체 경로 반복 실행에서 정상 대표 ID의 근거 없는 변경 0건.
- 해당 환경에서 불가로 확인한 후보의 자동 신규 채택 0건.
- UNKNOWN 때문에 발생한 정상 매핑 삭제·자동 대체·누락 게시 0건.
- 선택 결과·저장 결과·게시 예정 목록의 ID 불일치 0건.
- 실제 게시 성공과 게시 실패 상태의 정확한 구분, 실패 주입 후 복구 및 재실행 성공.
- 제보 5곡에 대해 이전 선택 복원 또는 근거 있는 대체·유지 판정 완료. 나머지 8곡과 조사에서 확장된 대상도 각각 정상/교정/미해결 근거가 있어야 하며 미해결을 완료 수에 포함하지 않음.
- 404처럼 이미 정상인 목록 항목의 불필요한 제거·추가 0건.
- 지정한 과거 날짜의 ID·metadata·Hype·NEW·등락·추이 오류 교정, 의도한 간접 순위 변화 전수 설명, 공개 history와 최신 실제 Hype 목록의 같은 snapshot 확인.
- 제한된 복구 범위 밖 곡·수동 정책·원본 순위·과거 화면 변화 0건. 과거 실패 이력의 성공 위장 0건.
- DB commit 직후·목록 사이·history 게시 중단 후 재개 성공, 오염 백업 복원을 정상 회복으로 보고한 경우 0건.
- 관련 Melon 게시 실패의 현재 상태 확인과 필요한 별도 복구 마감.
- 재실행, 일반 일일 작업, 월요일 주간 작업에서 안정성 확인.

최종 보고에는 `조사 곡/연결 수`, `수정 필요·정상 확인·보류 수`, `DB 교정 수`, `목록별 적용/no-op/검증 결과`, `정정 날짜와 필드`, `미해결 사유`, `재실행 결과`를 포함한다. 목록·날짜 단위 미해결이 하나라도 있으면 전체 성공 대신 부분 완료라고 기록한다.

API/목록 검증은 지정한 계정·환경에서 정확한 ID의 증거를 확인한 수준이다. 사용자가 보고한 재생 문제의 최종 수용 검사는 실제 사용하는 계정·지역·앱에서 복원된 목록 항목을 열어 재생 진행과 오디오를 확인하는 것이다. 지원되는 UI 관측 또는 사용자 확인으로 기록하며 Actions의 player 응답만으로 이를 대신하지 않는다.

계정/지역/YouTube 정책이 미래에 바뀌어도 영원히 재생된다는 보장은 완료 기준이 아니다. 완료 기준은 **정상 ID의 불필요한 교체를 막고, 검증된 장애는 일관되게 복구하며, 확인할 수 없는 상황에서는 기존 정상 상태를 훼손하지 않는 것**이다.

## 근거 자료

- [현재 저장된 이력 조사](/private/tmp/hype-wave-20260914-history-investigation.md)
- [현재 저장된 로컬 읽기 전용 실측](/private/tmp/hype-wave-20260914-probe.json)
- [메모리 SQLite 복구 저장 재현](/private/tmp/hype-wave-20260914-recovery-storage-repro.py)
- [선택 ID·저장 ID 불일치의 정제 결과](/private/tmp/hype-wave-20260914-recovery-storage-repro.json)
- [단일 저장 정책](/Users/yule/Documents/GitHub/hype_wave/hype_db_store.py:1624), [bulk 실행 저장의 commit 경계](/Users/yule/Documents/GitHub/hype_wave/hype_db_store.py:2843)
- [UID 병합과 기존 track 삭제](/Users/yule/Documents/GitHub/hype_wave/hype_db_store.py:978)
- [history의 공유 metadata 처리](/Users/yule/Documents/GitHub/hype_wave/hype_db_reports.py:130), [기본 export 범위](/Users/yule/Documents/GitHub/hype_wave/hype_db_reports.py:260)
- [날짜별 화면 hydrate](/Users/yule/Documents/GitHub/hype_wave/docs/index.html:677), [video ID 기반 등락 비교](/Users/yule/Documents/GitHub/hype_wave/docs/index.html:1057)
- [9/14 실행 이력](https://github.com/colinky/hype_wave/actions/runs/34843863675)
- [9/13 실행 이력](https://github.com/colinky/hype_wave/actions/runs/34758801774)

임시 경로의 자료는 구현 착수 시 허용 필드만 추출하여 저장소의 고정 fixture로 옮긴다. 임시 파일이 유지된다는 가정으로 회귀 검사를 설계하지 않는다.

## 11. 구현 단계에서 확장된 과거 오류 범위

2026-09-15 검증에서 원래 13곡의 복원 외에 MAGIC의 이용 불가 ID 1개와, Dirty Work·Seven·Love Love Love의 잘못된 녹음 연결 3건을 확인했다. 이 단계의 DB 명세는 17건이며, 전체 입력 검증에서 확인한 추가 7건은 §13에 포함한다. Dirty Work는 원곡의 Apple 연결만 기존 원곡 UID로 이동하고 featured 녹음은 보존한다. Seven의 Summer Mix와 Love Love Loveless는 원곡과 다른 UID로 분리한다. 이 분리에는 동일 곡 연결 영수증을 만들지 않는다.

17건 단계의 history 복구 미리보기는 원래 14행과 Dirty Work의 과거 10행, 총 24행·10날짜를 수정한다. 이 단계에서는 순위·점수·원본 서비스 정보를 보존하고, 서로 다른 녹음의 자동 연결을 막는 제외 관계를 유지한다. 추가 7건까지 포함한 최종 영향 범위와 필요한 점수 정정은 §13의 검증 결과를 따른다.

`publish_chart_repair.py`는 명세의 정확한 출력과 변경 전 항목 소유권을 사용해 게시·history 설치·공개 게시 확인을 분리한다. 기존 게시 감사 기록과 raw/선택 근거의 지문이 달라지거나 미해소 작업이 있으면 중단한다. 이미 정확한 결과가 반영된 재개에서는 추가 API 쓰기를 하지 않는다.

과거 match_attempts의 잘못된 선택을 새 값으로 덮어쓰거나 가짜 승인 기록으로 감추지 않는다. 정상 경로에서 고정된 전체 원본 입력을 새로 검증해 새 실행 기록을 만든 뒤 출력 일치 여부를 검증한다. PostgreSQL 저장 계약 시험과 이 일반 경로 검증은 별도 합격 조건이다.

## 12. 전체 입력 검증에서 발견한 원본·동일성 문제

전체 원본 입력 시험에서 재생 가능한 기존 곡도 앨범 출처 오염, 가수·제목 번역, 참여자의 식별자 누락 때문에 검증을 통과하지 못하는 사례를 확인했다. 오류를 무시해 게시하지 않고 다음 경계를 보완한다.

- Spotify 원본 앨범은 정확한 Spotify 곡 ID의 공식 응답에서만 가져온다. YouTube 검색 캐시나 MusicBrainz 앨범으로 원본을 보충하지 않는다. 공식 조회에서 비어 있는 앨범을 저장할 때도 과거 오염 값이나 matching 결과가 다시 주입되지 않아야 한다. 원본의 차트 순위·곡 ID는 유지한다.
- LEMONADE, Bloody Paradise, Ice Cream, DNA, Dynamite는 공식 카탈로그에서 같은 ISRC로 확인한 원곡의 앨범 수록 차이를 제한적으로 인정한다. 정확한 원본 ID·관측한 제목/가수/앨범·YouTube ID·길이로 범위를 제한한다. 다른 리믹스, 제목의 버전 표기, 참여진, 등급 차이를 무시하는 일반 규칙으로 확장하지 않는다. Spotify/YouTube 자체 ISRC를 직접 얻지 못한 연결은 공식 카탈로그를 통한 추론임을 기록한다.
- 가수의 언어별 표기는 같은 영상의 정확한 아티스트 ID나 공식 출처로 확인한 표기만 연결한다. 사랑으로/love. 제목 번역도 공식 Apple의 같은 곡 ID와 해당 원본·영상에 한정한다.
- 일부 공동 참여자의 ID가 없으면 신규 채택·ID 교체의 동일성 근거로 사용하지 않는다. 이미 같은 원본에 연결된 정상 ID를 유지하는 경우에만 인증된 같은 계정의 한국어·영어 응답, 연결된 주 아티스트, 모든 참여자 이름, 정확한 앨범의 곡 행, 원본 참여진을 함께 대조한다. 이름이나 앨범을 누락시키거나 명시적인 이용 불가 응답을 무시해 통과시키지 않는다.

추가 검증은 원본 앨범의 실제 저장 결과와 재실행 시 재오염 방지, 원곡/리믹스 반례, 누락·상충하는 언어별 정보, 인증되지 않은 조회, 기존 정상 ID 유지와 신규 교체의 구분을 포함한다. 전체 고정 입력에서 새 완료 실행을 만들고 다시 실행해 ID·연결·원본·출력 불변을 확인해야 운영 적용 단계로 진행한다. 이 검증 중 발견한 차단을 해소하기 전에는 일부 곡의 성공을 전체 성공으로 보고하지 않는다.

## 13. 전체 입력 검증 후 확정한 24건 복구와 추가 경계

전체 입력의 첫 시험은 NO-GO였다. 17건 교정만으로는 YouTube 원본의 일본어판·어쿠스틱·다른 Part·공연 버전 연결과 업로더 영상의 근거 없는 치환까지 해소되지 않았다. 다음 7개 원본 연결만 분리하여 DB 명세를 24건으로 확장한다. 다른 서비스가 사용하는 기존 UID·대표 영상·수동 정책은 보존한다.

| 원본 | 분리 이유 | 복구 경계 |
| --- | --- | --- |
| `0iiW__6izcs` | 일본어 DAY BY DAY가 한국어판으로 연결됨 | 일본어 원본만 분리 |
| `y5Wi5kOiGAQ` | RASEN 어쿠스틱이 스튜디오판으로 연결됨 | 어쿠스틱 원본만 분리 |
| `nhuFm3BGIBY` | Letter (feat. LYn)가 Part II로 연결됨 | 원본 분리와 확인된 앨범 180 DEGREE 교정 |
| `VvXVuWwAG90` | IF YOU 방콕 공연이 스튜디오판으로 연결됨 | Apple 스튜디오 연결을 보존하고 공연 원본만 분리 |
| `to3sjq-CAvA` | 업로더 원본과 외부 ATV의 동등성 미입증 | 다른 녹음이라고 단정하지 않고 원본 복원 |
| `Tq05jNIY_10` | 업로더 MV와 외부 ATV의 동등성 미입증 | 원본 복원, 가짜 음악 아티스트 증명 금지 |
| `UsjsYMo3O1Q` | 게스트 표기가 있는 OMV와 ATV의 동등성 미입증 | 원본 복원, 거짓 동일 곡 연결 금지 |

원본 ID 자체를 보존하는 예외는 같은 서비스 연결·같은 선택 ID·현재 정상 재생·정확한 watch 항목·비어 있지 않은 저장 metadata에 한정한다. 조회 전후 player의 제목·가수·영상 유형도 같아야 한다. 이 캐시는 다른 ID로 교체할 때 기존 음원의 독립적인 동일성 증거가 되지 않는다. 신규 ID, 수동 선택, 타 서비스 연결, 후보 교체는 기존의 엄격한 검사를 거친다.

업로더 원본 분리는 정확한 영상 ID·creator channel ID·player 제목/author/길이·한국어/영어 watch 행을 함께 보관한다. 조회수·좋아요 같은 식별자가 없는 정확한 숫자 표시만 제외하며, 추가 업로더나 이름을 무시하지 않는다. 일반 음악 아티스트 검증으로 위장하지 않고, 다른 플랫폼이나 다른 ID의 교체 승인에 재사용하지 않는다. metadata 기본값도 관측한 언어별 값 중 하나여야 한다.

전체 입력에서 확인한 추가 동일성 근거는 다음처럼 제한한다.

- 공식 출처로 확인한 18개 가수 표기를 추가한다. 동명이인·소속 표기·참여자 역할을 일괄 제거하지 않는다. 강민정과 김민정/Winter는 연결하지 않는다.
- TOESA의 Narr./feat. 차이는 확인한 두 Apple 곡 ID와 정확한 YouTube 영상에 한정한다. feat와 내레이션 역할을 일반적으로 동등하게 취급하지 않는다.
- Who·WDA의 앨범 수록 차이는 같은 ISRC의 공식 카탈로그와 정확한 원본/영상 조합으로 제한한다.
- BLUE는 이미 알려진 235.107초를 누락시키지 않는다. 같은 ISRC의 공식 233.099초 표준판과 정확한 233초 영상의 관측을 별도로 명시한다. 이 증명이 없으면 기존 ±2초 규칙으로 차단하며, 임의의 길이 허용 오차 확대로 대체하지 않는다.
- Spotify는 캐시가 모두 있어도 공식 한국어 원본을 다시 수집한다. 한국어 조회 실패를 과거 앨범이 교정됐다는 증거로 사용하지 않는다.

24건 history 미리보기는 2026-08-15~09-14의 31일, 5,987행이다. 기존 17건의 정정을 유지하면서 추가 원본 7개가 잘못 연결된 137행을 정정한다. IF YOU의 공연/스튜디오가 합산됐던 9일에는 기존 0.4/0.4/0.2 점수 정책으로 두 행을 나누므로 9행이 추가된다. 기존 행의 순위 변화는 312건이며, 그중 303건은 순위만 변하고 나머지 9건은 스튜디오 점수에서 공연 기여분도 분리된다. 순위만 변하는 행에 identity metadata를 새로 주입하지 않는다. 일본어판에 잘못 붙은 한국어 Melon URL은 제거하되 실제 Melon의 원곡 연결은 유지한다.

V3는 공개 원본·과거 raw 서비스 ID/기간/순위에서 별도로 기대값을 계산한다. 제품 복구 함수와의 차이 0, 실제 프런트 카드 584회와 31일×4종 차트 124회, 버전 분리·추이·제외 관계 반례를 확인했다. 이는 비공개 미리보기 검증이며 운영 복구 완료를 뜻하지 않는다.

최종 고정 revision에서 오프라인 검사, Actions 인증 환경 55개 ID의 반복 관측, 24건 격리 PostgreSQL 적용/롤백/재개, 전체 입력의 두 번 동기화와 heal을 다시 검증한다. Spotify Indie의 실제 검색 실패 2곡은 정직한 unmatched 기록으로 남기며 임의의 영상을 승인하지 않는다. 운영 적용은 이 검증을 통과한 뒤 DB → 11개 실제 목록 → history 공개 확인 순서를 따른다. 이전의 만료된 17건 명세나 임시 성공 결과를 최종 적용 근거로 사용하지 않는다.

## 14. 실제 캐시 경로 및 게시 상태 대조에서 발견한 추가 문제

`283ee8c`의 오프라인 530개 검사, Actions의 동일 검사와 55개 ID 두 차례 관측, 24건 PostgreSQL 격리 시험은 통과했다. 그러나 실제 공통 캐시 → 검색 → 최종 검증 경로를 사용한 전체 입력 시험의 첫 Hot Hits 작업은 NO-GO였다. 이 결과를 앞선 성공에 섞지 않으며, 수정 후 새 고정 revision으로 다시 검증한다.

- Who의 정확한 영상·길이와 한국어/영어 metadata는 모두 기존 증거와 일치했으나, 영어 client의 기본 가수 표시 `Jimin`이 한국어 수집 증거의 기본 표시 `지민 (Jimin)`과 달라 거절됐다. 언어별 literal 값은 계속 모두 대조하고, 기본 표시만 그 관측된 언어별 값 중 하나임을 확인한다. 임의의 기본 제목·가수·앨범이나 다른 영상은 허용하지 않는다.
- Hot Hits 100개 중 8개가 오래된 캐시 비교 규칙에서 탈락해 이미 정상인 대표 영상을 다시 검색했다. 최종 동일성 정책과 캐시 정책의 불일치를 공통 경로에서 해소한다. 저장된 연결 자체를 승인 근거로 삼지 않고, 현재 재생 판정과 기존 대표 영상의 정확한 동일성 검증을 완료한 결과만 캐시로 사용한다. 수동 정책 우선, UNKNOWN 중단, 이용 불가 기존 영상의 별도 대안 검색, 연결 변경 재조회는 유지한다.
- 첫 작업은 600초 판정 예산 소진 뒤에도 후보 처리·검색 정리에 약 106초를 더 사용했다. 예산이 소진됐거나 실행 환경이 불확실하면 새 자동 검색과 재시도 대기를 시작하지 않도록 검사한다. 실패를 정상 unmatched로 바꾸지 않는다. 이 실패 실행은 새 match_attempts와 source 완료 기록을 생성하지 않았고, 실패 작업 전후 시험 DB bytes도 같았다.

실제 11개 목록 대조에서는 DB에 정상 원곡이 남았지만 과거 다른 검색 결과가 게시된 Hot Hits 3항목도 확인했다. Beauty And A Beat의 Radio Mix, Seven의 원본 Explicit 표기와 다른 게시 선택, Love Love Love의 77초 orchestra 항목이다. Beauty는 DB 추가 교정 없이 목록만 정정하며 Seven/Love는 기존 복구 사례의 게시 교정을 포함한다. 과거 검색·게시 기록은 지우지 않는다.

incident 게시 미리보기는 11개 목록 1,044항목 중 49개 교체와 995개 기존 항목 토큰 보존이다. Melon의 이미 정상인 다른 수록판 5항목은 이 단계에서 보존하고, 후속 일반 동기화가 DB 대표 ID로 일치시킬 수 있음을 별도로 기록한다. 영구적인 예외 override는 만들지 않는다. Hype의 404와 BANG BANG은 실제 목록이 이미 정상 ID이므로 토큰까지 보존한다. 공개 history의 과거 잘못된 ID와 현재 실제 목록이 반드시 같다고 가정하지 않는다.

검증 담당은 실제 캐시 미스 8개를 별도 읽기 전용 시험으로 먼저 확인하고, 그 뒤 전체 두 회차·heal·11개 출력·과거 history 보존을 검사한다. 만료된 재생·업로더 증거는 실제로 재수집하고, 코드와 명세를 다시 고정한다. 이 단계의 계획·미리보기·격리 시험을 운영 복구 완료로 표시하지 않는다.

실제 PostgreSQL의 전체 DB 지문 확인은 세 차례 6.109~6.768초였다. 검토한 49개 이동 계획은 성공 경로에서 이 확인이 105회 필요하므로 같은 속도라면 확인만 641~711초가 걸린다. 전체 게시에 하나의 600초 관측 예산을 쓰지 않고, 각 목록의 게시 전과 전체 게시 후 검증에 새 관측 단계를 시작한다. 각 단계는 계정·기준 3곡·해당 대상 영상을 실제로 다시 확인하며 기존 성공 캐시나 관측 시간을 이식하지 않는다. DB·수동 정책·항목 소유권 확인은 모두 유지하고, UNKNOWN이나 미확정 게시를 새 단계로 우회하지 않는다. 최종 목록의 관측 시간은 실제 목록을 읽은 시각으로 남긴다.

느린 DB 확인 중 예산이 끝날 수도 있으므로 실제 외부 쓰기 직전에 같은 단계의 상태를 다시 검사한다. 미확정 항목 이동이나 복구 필요 상태는 일반 게시 실패와 구분하여 전체 실행과 단독 Hype 실행 모두에서 이후 게시·history 생성을 중단한다. 이전 목록으로 복구가 확정된 전송 실패를 별도 재시도 대상으로 기록하는 기존 정책은 유지한다.

## 15. 전체 실제 입력 재검증과 최종 적용 조건

`7a66ff3`의 오프라인 560개 검사, Actions 반복 진단, 24건 PostgreSQL 격리 검사는 통과했다. 실제 공통 캐시와 원본 950항목을 사용하는 첫 회차는 10개 작업 중 7개 완료·3개 차단으로 NO-GO였다. 이 실행의 두 번째 회차·heal·11개 출력 확정·history 생성은 진행하지 않았다. 아래 근거를 보완한 새 고정 revision에서 전체 경로를 다시 검사한다.

- hamo/하모의 정확한 영상 `LZWlP4kjEtw`는 현재 정상 재생되지만 player와 metadata의 아티스트 표시가 달라 차단됐다. player의 exact ID가 같고, metadata의 유일한 완전한 아티스트 ID에 모든 기본/한국어/영어 표시가 원문 그대로 등록된 경우에만 player 표시 차이를 인정한다. source 매칭이나 다른 ID 채택에는 사용하지 않는다. 제목·참여자 역할·버전·등급·알려진 길이 검사는 계속 적용한다. 다른 언어의 올바른 표시가 변조된 기본 표시를 가리는 반례도 차단한다.
- `night night`의 한국어 윤지영/영어 Whys Young 표기는 공식 Spotify의 동일 아티스트 ID와 본인의 공식 링크로 확인한 개명 근거로 한정한다. 주 가수 Andr와 게스트 역할, 정확한 음원은 별도로 검증한다.
- Indie의 나머지 입력을 개별 조사해 `Lay Down On The Grass`의 장한나/Hannah Jang 표기도 추가로 확인했다. 공식 Apple 아티스트 `827566406`의 한국어 이름과 정확한 곡 `6800257224`의 Feat. Hannah Jang 연결, Spotify 원본의 아티스트 ID 및 220초 음원 관측을 함께 사용한다. 이름의 어순만으로 같은 인물이라고 추정하지 않는다. 두 게스트 이름 보완의 독립 반례 32개가 통과했으며, 개별 분할 조사를 전체 작업의 600초 예산 통과로 간주하지 않는다.
- YouTube 차트의 `퇴사할게여`는 짧은 차트 제목에 내레이터가 생략됐다. 정확한 원본 `7mDDM0eBWR0`의 공식 뮤직비디오 제목에는 소연과 Narr. 기안84가 명시돼 있고, 연결된 `nxdxwZztGj0`의 한국어/영어 정확한 항목에도 같은 크레딧이 있다. 원본 player는 211초, microformat은 212초이며 ATV는 212초다. 이 차이를 그대로 기록하고, 기존 공식 카탈로그 211.575초와 함께 해당 원본/후보의 제한된 크레딧 근거로만 사용한다. 음악 영상과 음원이 항상 동등하다는 규칙이나 음성 비교·YouTube ISRC 확인으로 보고하지 않는다.

검증용 목록에서는 정상 ACK 이후 실제 4항목에 `trackCount=5`가 함께 반환됐다. 항목 구조·ID·고유 토큰을 먼저 검사한 뒤, 항목 전체가 해당 작업의 정확한 이전/이후 상태일 때만 기존 3회 범위 안에서 읽기를 재시도한다. 곡수가 다른 응답 자체를 성공으로 승인하지 않는다. 낯선 ID·토큰·순서가 섞인 응답은 미확정 게시로 보류하고 추가 삭제·추가·복구를 실행하지 않는다. 계속 지연되면 기존 durable 복구 절차로 넘긴다. 앞선 실제 시험은 별도 읽기 대조 후 0회 쓰기로 완료 상태를 확정하고 원래 검증 목록으로 복원했으며, 중단 없는 성공으로 표시하지 않았다.

Apple 200곡은 다른 작업의 metadata 캐시가 없는 새 프로세스에서 실제 캐시 200개·검색 0개·새 검증 기록 200개로 492.6초에 완료했다. 이는 기본 600초 경계의 단일 작업 검증이다. 전체 시험의 작업 간 캐시 재사용은 일반 작업의 별도 프로세스 실행보다 빠를 수 있으므로, 이 결과를 전체 실운영 소요 시간으로 사용하지 않는다. 정상 동기화에서는 정확한 언어별 조회에 따라 화면용 제목·가수·앨범이 갱신될 수 있다. 대표 ID·서비스 연결·alias 소유권·원본 순위·과거 실패 기록의 보존과 모든 표시 metadata의 불변을 혼동하지 않는다.

다음 체크포인트는 새 revision의 오프라인/Actions/격리 PostgreSQL 검사, 950개 고정 원본의 두 회차 완료와 heal, 11개 출력 및 과거 history 보존이다. 실제 검색이 실패하는 원본은 실패 기록으로 남기고, 새 차단이 나타나면 원인과 복구 범위를 먼저 재검토한다. 모두 통과한 뒤 최신 근거와 변경 전 상태를 다시 수집하여 운영 명세를 고정하고, 재발 방지 코드 배포 → 24건 DB 적용 → 11개 목록 → 공개 history 확인 → 일반 전체 동기화 순서로 진행한다. 최종 보고에는 중단·재개·실제 앱 재생 확인의 한계를 포함한다.

## 16. 복구 관측의 표시 필드와 실제 통신 실패 검증

`280387d`는 오프라인/Actions 571개 검사, 55개 ID의 두 차례 진단, 24건 PostgreSQL 리허설을 통과했다. 검토된 11개 목록과 31일 history의 고유 ID 533개도 모두 현재 playable이었다. Indie는 빈 metadata/아티스트 캐시의 새 프로세스에서 원본 100항목·고유 원본 97개, 실제 실패 검색 2건을 포함해 541.962초에 완료했다. 600초는 매칭 검증 단계의 예산이며, 앞선 공식 Spotify 원본 수집 시간까지 포함한 전체 수집기 제한으로 보고하지 않는다.

같은 revision의 전체 입력 시험에서는 실제 읽기 시간 초과·연결 종료·DNS 조회 오류가 발생해 Apple 작업 3개가 차단됐다. 해당 작업들은 DB bytes를 변경하지 않았으며, 이후 연결이 회복돼 완료된 다른 작업의 성공으로 실패를 덮어쓰지 않았다. 새 수정 검증을 위해 이 실행을 중단했고, 5개 완료 작업의 425개 새 기록과 실패 관측을 보존했다. heal·11개 출력 확정·history·두 번째 회차는 미실행이며 전체 결과는 NO-GO다.

검증용 목록은 삭제·추가·이동의 ACK를 보존하고 중복 요청 없이 명시적 복구로 원래 5곡을 복원했다. 실제 `tracks=4/trackCount=5` 응답은 관측됐지만, 뒤이은 두 읽기의 비교 실패는 당시 raw 응답을 모두 보존하지 않아 어떤 필드가 달랐는지 미확인이다. 이를 이전 순서의 지연이나 아래 재현 오류로 단정하지 않는다.

별도 오프라인 재현에서는 `isAvailable=True`와 필드 누락만 달라도 복구 코드가 목록 변경으로 오판하는 문제가 확인됐다. 복구 전후의 소유권은 정확한 ID·토큰·순서로 비교하고, 재생 가능 여부는 각 관측의 실제 표시와 기존 player 검증으로 검사한다. 대상 곡의 False 또는 불확실 판정은 계속 차단한다. 또한 명시적 이동 확인의 두 번째 관측에만 대상 곡의 False가 있을 때도 복구 claim과 후속 변경 전에 보류한다. 삭제할 비대상 기존 곡의 False 때문에 승인된 정상 대상 복구를 막지 않는다. 최종 감사 처리는 마지막으로 실제 검증한 항목을 사용하며, 저장소의 기존 ID·토큰 정규화 형식은 유지한다.

이 수정 후 코드를 다시 고정하고 오프라인/Actions·격리 DB·실제 복구 검사를 수행하며, 전체 고정 입력의 두 회차 시험을 처음부터 실행한다. 통신 실패가 재발하면 기존 정상 상태를 유지하는 차단 결과를 기록하고, 불완전한 회차를 전체 성공으로 보고하지 않는다.

## 17. 전체 출력 검증에서 확인한 과거 보조 차트의 선택 근거

`63c9aaf`의 오프라인·깨끗한 커밋 사본·Actions 회귀 574개, 반복 재생 진단, 24건 PostgreSQL 리허설은 통과했다. 실제 검증용 목록은 중간에 한 번 과거 순서를 반환한 응답을 기록했고, 추가 변경을 차단한 뒤 명시적 완료 0회 쓰기와 재실행으로 정상 상태를 확인했다. 중단 없는 게시 성공으로 집계하지 않는다.

같은 revision의 전체 원본 950항목 시험은 10개 작업 모두 완료해 947개의 새 매칭 기록을 만들었다. Indie는 실제 검색 실패 2건을 포함해 546.97초 안에 완료했다. 정상 대표 ID·alias 소유권·서비스 연결·원본 순위와 과거 시도는 유지됐고, 후처리의 변경도 0건이었다. 그러나 Hype 출력 확정 단계에서 선택 근거 누락으로 중단돼 전체 결과는 NO-GO다. 두 번째 회차와 일반 history export는 미실행이다.

최신 Gen Z combined의 해당 곡은 정상 매칭 근거를 갖고 있었다. 누락은 별도로 선택된 2026-08-28의 gen10·gen20 migration snapshot이었다. 이 보조 raw에는 개별 매칭 시도가 없으며, 실제 수집기도 두 보조 차트는 raw로 저장하고 combined만 매칭한다. Hype 검증 대상에는 이 과거 보조 행 154개가 들어왔지만, 실제 report와 기본 조회는 매칭된 상태의 행만 사용하므로 이 행들은 출력에 쓰이지 않는다.

수정은 출력에 실제 쓰이는 행과 필수 선택 근거를 구분한다. 매칭된 행끼리 먼저 그룹을 만들어 점수를 갖는 곡과 그 곡의 모든 표시·링크·재생 ID 보조 행을 검증한다. 양수 점수를 만들 수 있는 필수 원본의 선택 근거가 없으면 계속 중단한다. 사용되지 않는 보조 raw가 그룹을 연결하거나 대표 ID·표시를 선택하게 하지 않는다. 과거 raw·시도를 삭제하거나 가짜 매칭 기록을 만들지 않는다.

읽기 전용 진단에서는 선택 근거가 없는 보조 raw만 제외했을 때 11개 출력의 ID·순서, Hype 186행의 순위·점수, 최신 history의 공개 필드, 이전 30일의 모든 펼친 행과 identity 제외 관계가 승인된 기대값과 일치했다. 이 진단은 원래 실패 실행을 성공으로 바꾸지 않는다. 수정 후 실제 SQLite 회귀와 누락된 필수 근거·잘못된 보조 ID·그룹 연결 반례를 독립 검증하고, 새 고정 revision의 전체 반복 실행을 통과한 뒤 운영 적용한다.

## 18. 중단된 복구의 기존 항목 토큰 보존

`a8bbf4a`의 오프라인·깨끗한 커밋 사본·Actions 회귀 579개와 55개 ID의 반복 진단은 통과했다. 인증 관측 두 번 모두 47개 playable·8개 unavailable로 이전 기준과 같고, public 관측과 비인증 반례는 unknown을 유지했다. 24건 PostgreSQL 격리 시험도 통과했다. 이 결과를 실제 검증 목록의 중단 없는 성공이나 운영 복구 완료로 표시하지 않는다.

같은 revision의 검증 목록에서는 첫 곡 삭제 ACK 뒤 읽기 곡수 불일치로 복구가 시작됐고, 해당 곡을 끝에 다시 추가한 ACK까지 남았다. 추가 읽기도 불일치해 보류됐으며, 이후 세 번의 읽기에서 기존 네 항목과 재추가된 한 항목의 정확한 ID·토큰·순서를 확인했다. 명시적 복구의 기존 경로는 이 다섯 항목을 모두 삭제·재추가할 계획이어서 실제 적용 전에 중단했다. 곡의 재생 ID가 같아도 정상 기존 항목 토큰이 바뀌므로 승인된 995개 토큰 보존 계약을 만족하지 않는다.

명시적 원상 복구도 일반 게시와 같은 항목 보존 함수를 사용한다. 이미 존재하는 정상 항목은 토큰을 유지하며, 필요한 삭제·누락 추가·순서 이동만 durable intent/ACK와 현재 관측으로 검증한다. 관측된 다섯 항목의 복구는 재추가된 곡의 한 번 이동으로 끝나야 한다. 원래 삭제된 곡의 이전 토큰을 복원했다고 보고하지 않으며, 현재 소유권이 입증된 재추가 토큰과 나머지 네 토큰을 보존한다.

중복 원본 ID의 변경 복구는 기존 함수도 안전하게 지원하지 못했다. 새 경로에서는 claim 전에 보류하며, 이미 정확한 중복 목록은 항목 변경 없이 관측만으로 복구 완료를 확정할 수 있다. 운영 대상 11개 목록은 모두 고유 ID 목록이다. 낯선 토큰·불확실한 재생·관측 변경은 계속 차단하고, 이동 응답을 잃으면 앞선 삭제·추가·이동을 재실행하지 않는다. 같은 중단 상태를 격리 SQLite와 가짜 provider로 재현해 한 번 이동, 생존 토큰 보존, 누락·추가 혼합 복구, 중복 보류 및 응답 손실 반례를 검사한다. 새 revision을 고정한 뒤 실제 검증 목록을 명시적으로 완료하고 전체 입력 반복 검증을 다시 수행하며, 최초 중단과 후속 완료 결과를 각각 보존한다.

## 19. 실제 적용 결과와 Music 자동 치환에 따른 추가 복구 조건

`e7025de`의 오프라인·깨끗한 커밋 사본·Actions 584개 검사, 인증 재생 반복 관측, 24건 PostgreSQL 격리 검사, 고정 원본 950항목의 전체 두 회차는 통과했다. 두 회차는 각각 947개의 새 시도를 만들고 후처리 변경 없이 11개 출력을 확정했다. 이는 운영 목록에 쓰지 않은 격리 실행이다. history의 펼친 행은 보존됐지만 생성 시각이 달라 파일 바이트 전체가 같은 것은 아니다.

재발 방지 코드를 main에 배포한 후 실제 PG 명세로 DB 24건/27개 결정을 commit하고 독립 재접속 검증을 통과했다. 최초 시도의 SQLite/PG 정렬 차이는 값·타입·행 집합이 같은 4개 사례의 7개 배열에 한정됐으며 commit 전 롤백했다. 비교를 느슨하게 바꾸지 않고 실제 PG에서 새 명세를 생성해 그 명세 자체로 적용·롤백을 검증했다.

**전체 운영 판정은 NO-GO다.** 첫 Apple 200곡 목록에서 10개 삭제·10개 추가 성공 응답 뒤, 요청 `GxChUrrY4bc`의 새 토큰을 Music이 `JJx_WQXOeK0`로 반환했다. 기존 190개 항목과 나머지 새 9개는 보존했지만 이동은 실행하지 않았고 새 항목은 끝에 남았다. 기존 불가 곡으로 되돌리는 복구도 차단됐다. 07:14 KST 재조회에서 다른 10개 목록은 수정 전과 같고, 공개/로컬 history도 이전 SHA였다. 일반 전체 동기화는 아직 실행하지 않았다.

원본 Music 응답의 항목·삭제·재생 필드 모두 JJx였으므로 단순 파서 문제로 볼 수 없다. YouTube 웹에는 Gx가 표시됐지만 공개 웹의 일부 항목이 숨겨지고 토큰도 없어 전역 저장 ID가 덮어써졌다고 단정하지 않는다. 같은 제공자의 클라이언트별 ID 표현이 다르며, Music의 영상/음원 관계도 확인됐다.

실제 시험 목록에서는 원본 7개 모두 요청 ID로 추가 성공 응답을 받았지만 Music 조회에서 4개가 달랐다. 일본어 DAY BY DAY→한국어 버전은 명백히 거절해야 한다. RASEN은 이전 스튜디오 음원이 아닌 새로운 Acoustic 음원 `CTdGQIeWQM0`로 연결됐고 특정 발매 근거를 확보했다. 유리의 숲의 206/200초 공연과 Brain Rot의 Teto 참여 음원은 아직 동일 녹음 승인 근거가 충분하지 않다. 나머지 3개는 원본 ID를 유지했다. 시험에 추가한 7개만 영수증 토큰으로 제거하고 기존 5곡을 복구했으며, 이 시험은 성공으로 바꾸지 않고 실패로 종결했다.

따라서 §13 이후의 원본 ID 보존만으로 모든 과거 오류를 해소할 수 있다는 가정을 수정한다. 일반적인 길이 허용치를 늘리거나 모든 counterpart를 허용하지 않는다. 원본 ID를 요청했다는 사실과 실제 Music에서 듣게 되는 녹음이 일치하는지를 별도 체크포인트로 추가한다.

후속 작업은 (1) 표현이 달라진 각 곡의 공식 녹음·버전 검토, (2) 원래 24건 명세와 적용 영수증을 보존하는 후속 명세, (3) 이전 실패와 새 보상 결정을 연결하는 구현, (4) 부모/후속 범위·누락·순환·stale·소유권·UNKNOWN 반례, (5) 실제 Music 시험, (6) 정확한 PG 명세 리허설과 적용, (7) 11개 목록·공개 history 대조, (8) 복구 후 실제 일반 전체 실행 순서다. 이 후속 명세와 전체 복구 구현은 아직 완료하지 않았다. 별도로 가능한 공통 보호 보강은 §20처럼 구현·배포했다.

후속 명세가 한 곡만 수정하더라도 원래 전체 11개 목록·31일 history 및 변경하지 않은 사례까지 확인해야 기존 복구를 종료할 수 있다. 원래 요청·ACK·오류를 성공으로 소급하지 않고, 새 목표·실제 토큰 기준으로 변경/보존 수를 다시 계산한다. 기존 history identity를 계승하며 반대 방향 ID 연결을 추가해 순환을 만들지 않는다. 원본 일본어 버전을 끝내 정상 게시할 수 없을 때 제외하는 것은 기존 전체 복원 계획의 변경이므로, 사용자 답변 전에 제외하지 않는다. 자세한 역할 분담·통과 조건·증거는 [최종 검증 보고서](YTMusic_ID_Stability_Final_Verification.md)에 둔다.

## 20. 추가를 먼저 검증하는 공통 보호 구현·배포

`33321aa`는 누락된 곡을 50개 청크씩 먼저 추가하고, 각 성공 응답의 실제 ID·토큰·순서 및 재생이 확인된 뒤 기존 항목 제거·이동을 수행한다. 치환, 판정 불가, 소유권 변경, 응답 손실이면 다음 청크와 기존 항목 제거를 차단한다. 명시적 복구도 같은 함수를 사용한다. 별도의 일반 alias 허용이나 DB 모델은 추가하지 않았다.

추가 성공 응답 후 거절 이벤트를 쓰기 전에 중단된 경우에도 실제 영수증의 전체 항목과 현재 ID를 비교해 치환을 다시 판정한다. 거절된 ID의 추가를 일반 완료 명령이나 원상 복구 명령으로 반복하지 않는다. 성공 응답을 잃은 작업은 현재 전체 ID가 목표와 같아도 미완료로 유지한다. 현재 소유권을 증명한 새 항목만 제거하여 정상 기존 항목을 유지하는 복구는 허용한다.

최신 코드와 깨끗한 커밋 사본의 명시 회귀는 각각 597개 통과했다. 독립 SQLite 13개 경계와 관련 회귀 85개도 통과했고, main의 [Actions 회귀](https://github.com/colinky/hype_wave/actions/runs/34905688447)가 성공했다. 최초 Apple 200곡의 저장된 실제 입력으로 격리 SQLite·가짜 provider에서 재현했을 때 새 10곡 추가에서 Gx→JJx가 발생하면 기존 200개를 보존하고 삭제·이동 0회로 멈췄다. 겹치는 검사 수는 합산하지 않는다.

실제 소유 시험 목록에서는 새 Gx 추가 한 번 뒤 JJx 치환과 항목 개수 불일치가 발생했다. 개수 검사에서 먼저 중단하여 원래 5개 ID·토큰·순서는 모두 유지됐다. 안정된 세 번 관측과 영수증으로 확인한 새 토큰 한 개만 제거해 기존 5곡과 시험 감사 미완료 0건을 확인했다. 게시 단계의 직접 치환 거절 이벤트 이전에 중단됐으므로 그 직접 치환 분기 검증은 미통과로 기록했다. 이후 명시적 복구 단계에서는 추가 영수증과 실제 항목을 비교하여 Gx→JJx 거절을 감사에 기록했다. 이 결과를 전체 운영 복구 성공이나 직접 분기의 실환경 통과로 바꾸지 않는다.

보강은 기존 항목을 지우기 전에 중단할 수 있게 하지만, 새 항목이 추가되는 것 자체나 여러 목록 전체의 부분 변경을 없애지는 않는다. 앞서 부분 변경된 운영 목록·history는 여전히 §19의 후속 명세와 검증이 필요하며, 과거 명세의 구현 지문을 새 코드에 맞춰 덮어쓰지 않는다.
